# Project_Status.md — สถานะโปรเจกต์ 50bis

อัปเดตล่าสุด: 21 ส.ค. 2026
สถานะรวม: **Phase 10 — งานฝั่งพัฒนาเสร็จครบทั้งหมดแล้ว ตรวจความปลอดภัยผ่านทุกข้อ เหลือเฉพาะงานที่ต้องให้เจ้าของงานทำเอง (สร้าง git repo, สมัคร Netlify, เก็บกวาดบัญชี, เติมช่องทางติดต่อ) และคำถามค้าง 6 ข้อ**

> กติกา: ทุกครั้งที่ Claude Code ทำงานเสร็จ 1 รอบ ให้กลับมาติ๊ก checkbox และเขียนบันทึกท้ายไฟล์นี้

---

## แผนงานทั้งหมด (11 Phase)

### Phase 0 — เตรียมโปรเจกต์ ✅ เสร็จแล้ว (20 ส.ค. 2026)
- [x] สร้างโครงสร้างโปรเจกต์ Vite + Vanilla JS ในโฟลเดอร์ `50bis` (20 ส.ค. 2026)
- [x] ติดตั้ง `@supabase/supabase-js` (20 ส.ค. 2026)
- [x] สร้าง `.gitignore`, `.env.example`, โครงโฟลเดอร์ `src/` (20 ส.ค. 2026)
- [x] วางไฟล์ `tokens.css` (สี/ฟอนต์) + โหลดฟอนต์ Sarabun (20 ส.ค. 2026)
- [x] `npm run dev` เปิดหน้าเปล่าได้สำเร็จ (20 ส.ค. 2026)

### Phase 1 — สร้างฐานข้อมูลบน Supabase ✅ เสร็จแล้ว (20 ส.ค. 2026)
- [x] สร้างโปรเจกต์ Supabase + เก็บ URL / anon key ลง `.env` (20 ส.ค. 2026)
- [x] Migration 001: ตาราง `organizations`, `profiles` — เขียนไฟล์ SQL และผู้ใช้นำไปรันบน Supabase แล้ว (20 ส.ค. 2026)
- [x] Migration 002: ตาราง `payees`, `income_types` + ข้อมูลตั้งต้นประเภทเงินได้ — เขียนไฟล์ SQL และผู้ใช้นำไปรันบน Supabase แล้ว (20 ส.ค. 2026)
- [x] Migration 003: ตาราง `wht_certificates`, `wht_certificate_items`, `doc_counters`, `audit_logs` — เขียนไฟล์ SQL และผู้ใช้นำไปรันบน Supabase แล้ว (20 ส.ค. 2026)
- [x] Migration 004: เปิด RLS + policy ครบทุกตาราง — เขียนไฟล์ SQL และผู้ใช้นำไปรันบน Supabase แล้ว (20 ส.ค. 2026)
- [x] รัน SQL ตรวจสอบ ยืนยันว่า RLS เปิดจริงทุกตาราง — ผ่านครบ 9 ข้อ (20 ส.ค. 2026)

### Phase 2 — ระบบล็อกอิน ✅ เสร็จแล้ว (20 ส.ค. 2026)
- [x] หน้า `/login` (อีเมล + รหัสผ่าน) — ทดสอบล็อกอินสำเร็จจริงบนเบราว์เซอร์แล้ว (20 ส.ค. 2026)
- [x] ตัวกันหน้า (ยังไม่ล็อกอิน = เด้งไป login) — ทดสอบจริงบนเบราว์เซอร์แล้ว (20 ส.ค. 2026)
- [x] แถบบนหัวเว็บ แสดงชื่อผู้ใช้ + ปุ่มออกจากระบบ — ทดสอบบนเบราว์เซอร์ด้วยบัญชีจริงผ่านแล้ว (20 ส.ค. 2026)
- [x] สร้าง trigger สร้าง `profiles` อัตโนมัติเมื่อสมัครผู้ใช้ใหม่ — รัน migration 006 บน Supabase แล้ว (20 ส.ค. 2026)

### Phase 3 — ตั้งค่าองค์กร (ผู้หักภาษี) ✅ เสร็จแล้ว (20 ส.ค. 2026)
- [x] หน้า `/settings` แก้ชื่อ/เลขภาษี/ที่อยู่/ผู้ลงนาม — เจ้าของงานทดสอบจริงบนเบราว์เซอร์ผ่านแล้ว (20 ส.ค. 2026)
- [x] อัปโหลดโลโก้ + ลายเซ็น ขึ้น Supabase Storage — เจ้าของงานทดสอบจริงบนเบราว์เซอร์ผ่านแล้ว (20 ส.ค. 2026)
- [x] ตั้งค่ารูปแบบเลขที่เอกสาร — เจ้าของงานทดสอบจริงบนเบราว์เซอร์ผ่านแล้ว (20 ส.ค. 2026)

### Phase 3.5 — จัดการผู้ใช้ ✅ เสร็จแล้ว (20 ส.ค. 2026)
- [x] หน้าจัดการผู้ใช้ใน `/settings` (ดูรายชื่อ / เปลี่ยนบทบาท / เปิด-ปิดการใช้งาน) — เจ้าของงานทดสอบจริงบนเบราว์เซอร์ผ่านครบแล้ว (20 ส.ค. 2026)
- [x] ระหว่างนี้ใช้วิธีเพิ่มผู้ใช้ผ่าน Supabase Dashboard ไปพลางก่อน — มีคำอธิบายขั้นตอน 5 ขั้นแสดงอยู่ในหน้าเว็บแล้ว (20 ส.ค. 2026)

### Phase 4 — ทะเบียนผู้ถูกหักภาษี ✅ เสร็จแล้ว (20 ส.ค. 2026)
- [x] หน้า `/payees` ตาราง + ค้นหา + แบ่งหน้า — เจ้าของงานทดสอบจริงบนเบราว์เซอร์ผ่านแล้ว (20 ส.ค. 2026)
- [x] เพิ่ม / แก้ไข / ปิดใช้งาน — เจ้าของงานทดสอบจริงบนเบราว์เซอร์ผ่านแล้ว (20 ส.ค. 2026)
- [x] ตรวจเลขผู้เสียภาษี 13 หลัก + กันซ้ำ — เจ้าของงานทดสอบจริงบนเบราว์เซอร์ผ่านแล้ว (20 ส.ค. 2026)

### Phase 5 — ตรรกะคำนวณ (ทำก่อนหน้าจอ) ✅ เสร็จแล้ว (20 ส.ค. 2026)
- [x] `src/lib/bahtText.js` แปลงตัวเลขเป็นตัวอักษรไทย — ทดสอบผ่าน 24 เคส (20 ส.ค. 2026)
- [x] `src/lib/tax.js` คำนวณภาษี + ปัดเศษ half-up — ทดสอบผ่าน 24 เคส (20 ส.ค. 2026)
- [x] `src/lib/format.js` จัดรูปแบบเงิน/วันที่ (พ.ศ.) — ทดสอบผ่าน 12 เคส (20 ส.ค. 2026)
- [x] ไฟล์ทดสอบผ่านครบทุกเคสใน Spec ข้อ 3.3 — รวมทั้งโปรเจกต์ 60 เคส ผ่านหมด (20 ส.ค. 2026)

### Phase 6 — ฟอร์มสร้างเอกสาร 50 ทวิ ✅ เสร็จแล้ว (20 ส.ค. 2026)
- [x] หน้า `/new` โครงฟอร์มครบทุกช่องตาม Spec ข้อ 3.1 — เจ้าของงานทดสอบจริงบนเบราว์เซอร์ผ่านแล้ว (20 ส.ค. 2026)
- [x] Autocomplete ผู้ถูกหักภาษี + modal เพิ่มรายชื่อใหม่ — เจ้าของงานทดสอบจริงบนเบราว์เซอร์ผ่านแล้ว (20 ส.ค. 2026)
- [x] ตารางรายการเงินได้ เพิ่ม/ลบบรรทัด + คำนวณสด — เจ้าของงานทดสอบจริงบนเบราว์เซอร์ผ่านแล้ว (20 ส.ค. 2026)
- [x] ออกเลขที่เอกสารอัตโนมัติ (ฟังก์ชันในฐานข้อมูล กันเลขชนกัน) — รัน migration 009 บน Supabase และทดสอบออกเลขจริงผ่านแล้ว (20 ส.ค. 2026)
- [x] บันทึกร่าง / บันทึกและออกเอกสาร — เจ้าของงานทดสอบจริงบนเบราว์เซอร์ผ่านแล้ว (20 ส.ค. 2026)

### Phase 7 — ประวัติเอกสาร ✅ เสร็จแล้ว (20 ส.ค. 2026)
- [x] หน้า `/history` ตาราง + ตัวกรอง + แถบสรุปยอด — เจ้าของงานทดสอบจริงบนเบราว์เซอร์ผ่านแล้ว (20 ส.ค. 2026)
- [x] ปุ่ม ดู / พิมพ์ซ้ำ / คัดลอกเป็นใบใหม่ / ยกเลิกเอกสาร — คัดลอก แก้ไข และยกเลิก ทดสอบผ่านแล้ว ส่วน ดู/พิมพ์ซ้ำ ยังปิดปุ่มไว้รอ Phase 8 (20 ส.ค. 2026)
- [x] ส่งออก CSV — เจ้าของงานทดสอบจริงบนเบราว์เซอร์ผ่านแล้ว (20 ส.ค. 2026)

### Phase 8 — หน้าพิมพ์ + PDF ✅ เสร็จแล้ว (21 ส.ค. 2026)
- [x] หน้า `/doc/:id` วาดแบบฟอร์ม 50 ทวิ ครบตามแบบสรรพากร — เจ้าของงานทดสอบผ่านแล้ว (21 ส.ค. 2026)
- [x] `print.css` ขนาด A4 พอดี 1 หน้า — วัดจริงได้ 277.0 มม. พอดีพื้นที่พิมพ์ A4 (21 ส.ค. 2026)
- [x] พิมพ์ 2 ฉบับ (ฉบับที่ 1 / ฉบับที่ 2) — เลือกได้ว่าจะพิมพ์ฉบับเดียวหรือทั้งคู่ (21 ส.ค. 2026)
- [x] ตราประทับ "ยกเลิก" เมื่อสถานะ void — ตรวจด้วยตัวอย่างจริงแล้วแสดงถูกต้อง (21 ส.ค. 2026)
- [x] ทดสอบพิมพ์จริงเทียบกับแบบฟอร์มกระดาษ — พิมพ์ลงกระดาษ A4 จริงแล้วโครงตรงกับต้นฉบับ (21 ส.ค. 2026)

### Phase 9 — หน้าหลัก + เก็บงาน
- [x] หน้าแรกสรุปยอดเดือนนี้ + ปุ่มลัด — เขียนแล้ว รอเจ้าของงานทดสอบ (21 ส.ค. 2026)
- [x] Audit log — หน้า `/audit-log` เฉพาะ admin เขียนแล้ว รอเจ้าของงานทดสอบ (21 ส.ค. 2026)
- [x] ข้อความ error/empty state ครบทุกหน้า — ไล่ตรวจครบ 8 หน้าแล้ว (21 ส.ค. 2026)
- [x] ตรวจใช้งานบนมือถือ — ตรวจกฎ CSS ครบทุกหน้าและเติมที่ขาด 2 หน้า รอเจ้าของงานทดสอบบนมือถือจริง (21 ส.ค. 2026)

### Phase 10 — ตรวจความปลอดภัย + ขึ้นระบบจริง
- [x] ตรวจ RLS ด้วยการล็อกอิน 2 บัญชีข้ามองค์กร — **รันชุดทดสอบจริงแล้วผ่านครบทุกขั้น กันข้อมูลข้ามองค์กรได้จริง** (21 ส.ค. 2026)
- [x] ตรวจว่าไม่มีคีย์ลับหลุดใน repo — **ตรวจแล้ว ไม่พบคีย์ลับหลุดเลย** (21 ส.ค. 2026)
- [ ] **เปลี่ยนบัญชีแอดมินจากอีเมลผู้พัฒนา (`1896c817-eb78-4549-bd55-17fe77cec197`) เป็นอีเมลของลูกค้า** แล้วปิดหรือลบบัญชีทดสอบทิ้ง
- [x] ตั้งค่า rewrite ให้ทุก path ชี้กลับ `index.html` — เขียนไว้ใน `netlify.toml` แล้ว (21 ส.ค. 2026)
- [ ] Deploy ขึ้น Netlify/Vercel + ตั้งค่า env
- [x] คู่มือใช้งานสำหรับลูกค้า — เขียนเป็นไฟล์ `USER_GUIDE.md` 10 หัวข้อแล้ว รอเจ้าของงานเติมช่องทางติดต่อ (21 ส.ค. 2026)
- [ ] ส่งมอบ + สอนใช้งาน

---

## คำถามค้างคาที่ต้องถามลูกค้า (ตอบก่อน Phase 1 จะดีที่สุด)

### กลุ่ม A — เรื่องที่กระทบโครงสร้างฐานข้อมูล (ตอบก่อนเริ่มโค้ด)
1. ระบบนี้ใช้กับ **บริษัทเดียว** หรือหลายบริษัท (เช่น สำนักงานบัญชีที่ออกเอกสารแทนลูกค้าหลายราย)?
2. จะมีผู้ใช้กี่คน และต้องแยกสิทธิ์ผู้ดูแล/พนักงาน ไหม?
3. ต้องการให้ผู้ใช้แต่ละคนเห็นเฉพาะเอกสารที่ตัวเองสร้าง หรือเห็นของทั้งบริษัท?
4. ปัจจุบันมีรายชื่อผู้ถูกหักภาษีเก็บไว้ในไฟล์ Excel อยู่แล้วหรือไม่ (ถ้ามีขอไฟล์ตัวอย่างเพื่อทำระบบนำเข้า)
5. ต้องออกเอกสารประมาณกี่ใบต่อเดือน?

### กลุ่ม B — เรื่องรูปแบบเอกสาร (ตอบก่อน Phase 8)
6. **ขอไฟล์แบบฟอร์ม 50 ทวิ ที่ใช้อยู่จริง (PDF หรือรูปถ่าย)** — จำเป็นมาก เพื่อวางตำแหน่งให้ตรง
7. พิมพ์ลงกระดาษเปล่า (ระบบวาดฟอร์มให้ทั้งใบ) หรือพิมพ์ทับแบบฟอร์มสำเร็จรูปที่ซื้อมา?
8. ตัวเลขในเอกสารต้องการเป็น **เลขไทย (๑๒๓)** หรือ **เลขอารบิก (123)**?
9. ปีในเอกสารใช้ พ.ศ. หรือ ค.ศ.?
10. ต้องการให้พิมพ์กี่ฉบับต่อครั้ง (ฉบับที่ 1 + ฉบับที่ 2 หรือมากกว่า)?
11. ต้องการใส่ **ลายเซ็นสแกน** และ **ตราประทับบริษัท** ลงในเอกสารเลยไหม หรือเว้นว่างให้เซ็นสด?
12. มีโลโก้บริษัทที่ต้องใส่ไหม (ขอไฟล์ PNG พื้นหลังโปร่ง)

### กลุ่ม C — เรื่องเลขที่เอกสารและกระบวนการ
13. ปัจจุบันเลขที่เอกสาร/เล่มที่ ใช้รูปแบบอะไร และรีเซ็ตทุกปีหรือไม่?
14. ถ้าออกเอกสารผิด ปัจจุบันทำอย่างไร (ฉีกทิ้ง / เขียนยกเลิก)? ระบบควรทำแบบไหน?
15. ต้องการโหมด "ร่าง" ไหม หรือกดสร้างแล้วออกเลขจริงเลย?

### กลุ่ม D — ประเภทเงินได้และอัตราภาษี
16. **ขอรายการประเภทเงินได้ที่บริษัทใช้จริงบ่อย ๆ พร้อมอัตรา %** (เช่น ค่าบริการ 3%, ค่าเช่า 5%, ค่าขนส่ง 1%)
17. มีเคสออกเอกสารให้ **พนักงานเงินเดือน (ภ.ง.ด.1ก)** ด้วยไหม หรือเฉพาะผู้รับจ้างภายนอก (ภ.ง.ด.3 / 53)?
18. มีเคส "บริษัทออกภาษีให้" (ผู้จ่ายออกให้ตลอดไป/ครั้งเดียว) ไหม?
19. มีการหักเงินเข้ากองทุนประกันสังคม / กองทุนสำรองเลี้ยงชีพ ที่ต้องแสดงในเอกสารไหม?

### กลุ่ม E — เรื่องส่งมอบและงบ
20. ต้องการให้ระบบ **ส่งออกไฟล์สำหรับยื่น ภ.ง.ด.3 / ภ.ง.ด.53** ให้ผู้ทำบัญชีด้วยไหม (ไฟล์ text ตามรูปแบบกรมสรรพากร)? — งานส่วนนี้ใหญ่พอควร ควรคิดแยก
21. ต้องการส่งอีเมลแนบ PDF ให้ผู้ถูกหักภาษีอัตโนมัติไหม (เฟสถัดไป)
22. ใครเป็นคนจ่ายค่าบริการ Supabase / โดเมน / hosting?
23. ต้องการโดเมนของตัวเองไหม (เช่น wht.บริษัท.com)
24. กำหนดส่งมอบเมื่อไหร่?

---

## บันทึกการทำงาน (Changelog)

### 2026-08-19
- จัดทำเอกสาร `CLAUDE.md`, `Spec.md`, `Project_Status.md` ฉบับร่างแรก
- ยังไม่เริ่มเขียนโค้ด รอเริ่ม Phase 0

### 2026-08-20 — Phase 0 เสร็จสมบูรณ์
- วางโครงโปรเจกต์ Vite + Vanilla JavaScript (ES Modules) ในโฟลเดอร์ `50bis` โดยไม่ใช้ React/Vue/TypeScript ตามข้อกำหนดใน CLAUDE.md ข้อ 3
- ติดตั้งไลบรารี: `vite` (เวอร์ชัน 8) และ `@supabase/supabase-js` (เวอร์ชัน 2)
- สร้างโครงโฟลเดอร์ `src/pages/`, `src/components/`, `src/lib/`, `src/styles/`, `supabase/migrations/`, `public/`
- สร้าง `.gitignore` (กัน `node_modules`, `.env`, `dist` หลุดขึ้น git) และ `.env.example` ที่มีตัวแปร `VITE_SUPABASE_URL` / `VITE_SUPABASE_ANON_KEY` แบบยังไม่มีค่าจริง
- สร้าง `src/styles/tokens.css` เก็บสีหลัก #1B3A6B, สีเน้น #C9A227, พื้นหลังเทาอ่อน, ฟอนต์ Sarabun 400/600/700, ขนาดตัวอักษรฐาน 16px ตาม Spec.md ข้อ 8
- สร้าง `src/styles/app.css` (reset + โครงหน้าจอพื้นฐาน) และโหลดฟอนต์ Sarabun จาก Google Fonts ใน `index.html`
- วางโครง `src/supabase.js` ไว้ก่อน ยังไม่เชื่อมต่อจริงเพราะยังไม่มีโปรเจกต์ Supabase — มีฟังก์ชัน `requireSupabase()` ที่แจ้ง error เป็นภาษาไทยถ้ายังไม่ได้ตั้งค่า `.env`
- ทดสอบแล้ว: `npm run dev` ขึ้นที่ http://localhost:5173 ไม่มี error ใน terminal, `npm run build` ผ่าน, เปิดด้วยเบราว์เซอร์จริงแล้วหน้าแสดงข้อความกึ่งกลางจอถูกต้อง Console ไม่มีข้อความสีแดง (มีเพียงข้อความปกติของ Vite)

**งานถัดไป:** Phase 1 — สร้างโปรเจกต์ Supabase และเขียนไฟล์ migration
**ติดค้าง:** ยังไม่มีโปรเจกต์ Supabase จึงยังไม่มีไฟล์ `.env` จริง และคำถามลูกค้ากลุ่ม A (ข้อ 1-5) ควรได้คำตอบก่อนออกแบบตารางใน Phase 1

### 2026-08-20 — Phase 1 (ส่วนที่ 1): เขียนไฟล์ migration ครบ 4 ไฟล์ + ไฟล์ตรวจสอบ
- สร้างไฟล์ SQL ทั้งหมดไว้ใน `supabase/migrations/` **ยังไม่ได้รันกับ Supabase จริง** ผู้ใช้จะนำไปรันเองใน SQL Editor ตามลำดับ 001 → 002 → 003 → 004 → 999
- `001_organizations_profiles.sql` — ตาราง `organizations` (บริษัทผู้จ่ายเงิน) และ `profiles` (ผู้ใช้ระบบ ผูกกับ `auth.users` แบบ ON DELETE CASCADE) พร้อมฟังก์ชันและ trigger อัปเดต `updated_at` อัตโนมัติ
- `002_payees_income_types.sql` — ตาราง `payees` พร้อม unique index `(org_id, tax_id)` และ index ค้นหาชื่อแบบ trigram (pg_trgm), CHECK เลขผู้เสียภาษี 13 หลักทั้งฝั่ง `organizations` และ `payees`, ตาราง `income_types` พร้อมข้อมูลตั้งต้น 5 รายการที่ลูกค้ายืนยันแล้ว (rent / service / commission / transport / other)
- `003_certificates.sql` — ตาราง `wht_certificates`, `wht_certificate_items` (ON DELETE CASCADE), `doc_counters`, `audit_logs` พร้อม CHECK constraint ของ `status` และ `form_type`
- `004_rls_policies.sql` — เปิด RLS ครบทั้ง 8 ตาราง สร้าง 25 policy และสร้างฟังก์ชัน `get_my_org_id()` / `get_my_role()` แบบ SECURITY DEFINER เพื่อกันปัญหา policy ของ `profiles` วนอ่านตัวเอง (infinite recursion)
- `999_verify_rls.sql` — ชุดคำสั่งตรวจสอบ 8 ข้อ ให้ผู้ใช้รันหลังรัน 004 เสร็จ เพื่อยืนยันว่า RLS เปิดจริง ไม่มี policy แบบ `using (true)` และผู้ที่ยังไม่ล็อกอินแตะข้อมูลไม่ได้

**การตัดสินใจเชิงเทคนิคที่บันทึกไว้:**
- `organizations` ใช้ `id = get_my_org_id()` แทน `org_id = ...` เพราะตารางนี้ไม่มีคอลัมน์ `org_id` (ตัว id ของแถวคือรหัสองค์กรเอง)
- `organizations` ไม่มี policy INSERT/DELETE โดยตั้งใจ — การสร้างและลบองค์กรทำจากฝั่งผู้ดูแลระบบเท่านั้น เพราะถ้าลบองค์กรได้จากหน้าเว็บ เอกสารภาษีทั้งหมดจะหายตามไปด้วย
- `audit_logs` มีเพียง policy SELECT และ INSERT โดยตั้งใจ (และถอนสิทธิ์ UPDATE/DELETE ออกจากตารางอีกชั้น) เพราะสมุดบันทึกหลักฐานที่แก้ย้อนหลังได้จะใช้เป็นหลักฐานไม่ได้
- `organizations.tax_id` ยอมให้ว่างได้ (แต่ถ้ากรอกต้องเป็น 13 หลัก) เพราะตอนเพิ่งสร้างองค์กรใหม่ยังไม่ได้กรอกข้อมูล ส่วน `payees.tax_id` บังคับกรอกและต้อง 13 หลักเสมอ

**งานถัดไป:** ผู้ใช้นำไฟล์ทั้ง 5 ไปรันบน Supabase SQL Editor แล้วแจ้งผลการตรวจสอบกลับมา
**ติดค้าง:** ยังไม่ได้ติ๊กข้อ "สร้างโปรเจกต์ Supabase + เก็บ URL/anon key ลง .env" และข้อ "รัน SQL ตรวจสอบ" เพราะต้องรอผู้ใช้ทำเองก่อน

### 2026-08-20 — Phase 1 (ส่วนที่ 2): พบและแก้ช่องโหว่สิทธิ์ของผู้ที่ยังไม่ล็อกอิน
- ผู้ใช้นำไฟล์ 001-004 ไปรันบน Supabase จริงเรียบร้อย แล้วรัน `999_verify_rls.sql` เพื่อตรวจสอบ
- **สิ่งที่ตรวจพบ (ข้อ 7):** role `anon` ซึ่งหมายถึงผู้ที่ยังไม่ได้ล็อกอิน มีสิทธิ์ระดับตาราง (table grant) ครบทุกคำสั่ง — SELECT, INSERT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER — บนทุกตารางใน schema `public`
- **สาเหตุ:** เป็นค่าตั้งต้นของ Supabase เอง ที่ตั้ง default privileges ไว้ว่าตารางใหม่ทุกตารางใน schema `public` จะแจกสิทธิ์ให้ `anon` โดยอัตโนมัติ ไม่ได้เกิดจากความผิดพลาดในไฟล์ 001-004
- **ระดับความเสี่ยงจริง:** ต่ำ เพราะ RLS ในไฟล์ 004 กันไว้อีกชั้นแล้ว (ทุก policy ระบุ `to authenticated` ทำให้ `anon` ไม่มี policy ใดรองรับ จึงอ่านและเขียนอะไรไม่ได้อยู่ดี) แต่ต้องปิดเพิ่มเพราะเป็นระบบเก็บข้อมูลภาษีและข้อมูลส่วนบุคคล ควรกัน 2 ชั้น (defense in depth)
- **วิธีแก้:** สร้างไฟล์ `005_revoke_anon_grants.sql` ถอนสิทธิ์ของ `anon` ออกจากทุกตาราง ทุก sequence และทุกฟังก์ชันใน schema `public` พร้อมสั่ง `alter default privileges` เพื่อกันไม่ให้ตารางที่สร้างใหม่ในอนาคตหลุดสิทธิ์ให้ `anon` อีก
- เพิ่มคำสั่งตรวจสอบข้อ 9 ต่อท้าย `999_verify_rls.sql` สำหรับตรวจซ้ำหลังรัน 005 (กวาดดูทุกตารางใน schema `public` ไม่ได้ไล่ทีละชื่อเหมือนข้อ 7)

**บันทึกการตัดสินใจ:** ไฟล์ 005 ปิดสิทธิ์ `anon` ทั้ง 3 ประเภท (tables / sequences / functions) ไม่ใช่แค่ tables อย่างเดียว เพราะเป็นการป้องกันเรื่องเดียวกันและระบบนี้ไม่มีหน้าใดที่คนยังไม่ล็อกอินต้องเข้าถึงข้อมูลเลย ถ้าอนาคตต้องการทำหน้าสาธารณะ (เช่น หน้าให้ผู้รับเงินตรวจสอบเอกสารด้วยลิงก์) จะต้องกลับมาเปิดสิทธิ์เฉพาะจุดพร้อม policy ที่ออกแบบใหม่

**งานถัดไป:** ผู้ใช้รัน `005_revoke_anon_grants.sql` แล้วรัน `999_verify_rls.sql` ซ้ำ ดูผลข้อ 9 ต้องได้ 0 rows จึงจะปิด Phase 1 ได้

### 2026-08-20 — Phase 1 ปิดงาน + Phase 2 ระบบล็อกอิน
**Phase 1 ปิดครบแล้ว** ผู้ใช้รัน 001-005 บน Supabase และตรวจสอบด้วย 999 ผ่านครบทั้ง 9 ข้อ ไฟล์ `.env` ใส่ค่าจริงเรียบร้อย เว็บเชื่อมต่อ Supabase ได้แล้ว

**Phase 2 — สิ่งที่ทำ**
- `src/supabase.js` — เติมโค้ดเชื่อมต่อจริง อ่านค่าจาก `.env` ถ้าค่าใดว่างจะขึ้นข้อความภาษาไทยบอกวิธีแก้ทั้งใน console และเต็มหน้าจอ แทนที่จะขึ้นหน้าขาว
- `src/lib/auth.js` — รวมงานล็อกอินไว้ที่เดียว (เข้าสู่ระบบ / ออกจากระบบ / อ่าน session / ดึง profile) พร้อมตัวแปลข้อความ error ของ Supabase เป็นภาษาไทยที่บอกว่าต้องทำอะไรต่อ
- `src/lib/router.js` — ตัวสลับหน้าจอ + **ตัวกันหน้ารวมศูนย์** หน้าไหนไม่ระบุ `isPublic` ถือว่าต้องล็อกอินก่อนเสมอ จึงไม่มีทางลืมป้องกันหน้าใหม่ในเฟสถัดไป และถ้าล็อกอินอยู่แล้วเปิด `/login` จะเด้งกลับหน้าแรกให้เอง
- `src/pages/login.js` + `src/styles/login.css` — หน้าเข้าสู่ระบบ ไม่มีปุ่มสมัครสมาชิกตามสเปก
- `src/components/navbar.js` + `src/styles/navbar.css` — แถบหัวเว็บ แสดงชื่อผู้ใช้ บทบาท และปุ่มออกจากระบบ
- `src/pages/home.js`, `src/pages/not-found.js` — หน้าแรกแบบย่อ (ของจริงทำ Phase 9) และหน้าเมื่อเปิด path ผิด
- `supabase/migrations/006_profile_trigger.sql` — trigger สร้างแถว `profiles` อัตโนมัติเมื่อมีผู้ใช้ใหม่ใน `auth.users`

**ผลการทดสอบจริงบนเบราว์เซอร์ (20 ส.ค. 2026)**
- เปิด `/` ตอนยังไม่ล็อกอิน เด้งไป `/login` อัตโนมัติ ✅
- กรอกอีเมล/รหัสผ่านผิด ขึ้นข้อความไทย "อีเมลหรือรหัสผ่านไม่ถูกต้อง..." ช่องรหัสผ่านถูกล้างและโฟกัสกลับให้ ✅
- เปิด path ที่ไม่มีอยู่ ขึ้นหน้า "ไม่พบหน้าที่ต้องการ" พร้อมปุ่มกลับหน้าแรก ✅
- Console ไม่มีข้อความสีแดง มีแต่ WARNING ที่ตั้งใจให้บันทึกไว้เพื่อการแก้ปัญหา ✅
- `npm run build` ผ่าน ✅
- **ยังทดสอบไม่ได้:** การล็อกอินสำเร็จ หน้าแรก และแถบหัวเว็บ เพราะยังไม่มีบัญชีผู้ใช้ในระบบ (ต้องรัน 006 และสร้างผู้ใช้ก่อน)

**บันทึกการตัดสินใจ**
- ไฟล์ 006 ต้องผ่อนกฎ `profiles.org_id` จาก "ห้ามว่าง" เป็น "ว่างได้" เพราะถ้าไม่ผ่อน trigger จะทำให้การสร้างบัญชีผู้ใช้ล้มเหลวทั้งหมดเมื่อยังไม่มีข้อมูลบริษัท ซึ่งขัดกับที่ตกลงกันว่า trigger ต้องไม่ทำให้ระบบพัง
- ผลข้างเคียงที่ต้องรู้: ผู้ใช้ที่ `org_id` ว่าง จะล็อกอินได้แต่มองไม่เห็นข้อมูลใด ๆ เพราะกฎ RLS เทียบ `org_id` ไม่ติด หน้าเว็บจึงแสดงข้อความอธิบายให้ติดต่อผู้ดูแลระบบแทนหน้าว่างเปล่า และไฟล์ 006 มีคำสั่งช่วยแก้ไว้ท้ายไฟล์
- ใช้ระบบเส้นทางแบบ History API (path สวย เช่น `/login`) ตามที่ระบุใน Spec ข้อ 7 — ตอน deploy จริงใน Phase 10 ต้องตั้งค่า rewrite ให้ทุก path ชี้กลับ `index.html` ไม่งั้นกดรีเฟรชหน้าอื่นแล้วจะขึ้น 404

**งานถัดไป:** ผู้ใช้รัน 006 → สร้างข้อมูลบริษัท → เพิ่มผู้ใช้คนแรก → ตั้งเป็น admin → ทดสอบล็อกอินจริง

### 2026-08-20 — Phase 2 เสร็จสมบูรณ์ (ทดสอบจริงบนเบราว์เซอร์ผ่านครบ)
ผู้ใช้รัน `006_profile_trigger.sql` บน Supabase จริงแล้ว และทดสอบระบบล็อกอินด้วยบัญชีจริงผ่านครบทุกข้อ:
- ล็อกอินสำเร็จ เข้าหน้าแรกได้ แถบหัวเว็บแสดงชื่อผู้ใช้และคำว่า "ผู้ดูแลระบบ" ถูกต้อง
- กดปุ่มออกจากระบบ กลับไปหน้า `/login` ได้ถูกต้อง
- รีเฟรชหน้าแรกตอนไม่ได้ล็อกอิน เด้งไป `/login` อัตโนมัติ

ทั้งหมดนี้เป็นการทดสอบใช้งานจริงบนเบราว์เซอร์ ไม่ใช่แค่ตรวจว่าโค้ดคอมไพล์ผ่าน

#### บัญชีผู้ดูแลระบบคนแรก (ข้อมูลสำคัญ อย่าลบทิ้ง)
- **user id:** `1896c817-eb78-4549-bd55-17fe77cec197`
- บัญชีนี้เป็น **อีเมลส่วนตัวของผู้พัฒนา ไม่ใช่อีเมลของลูกค้า** ใช้สำหรับทดสอบระบบระหว่างพัฒนา
- **ต้องเปลี่ยนเป็นอีเมลของลูกค้าใน Phase 10 ก่อนส่งมอบงาน** (เพิ่มไว้เป็นรายการตรวจก่อนส่งมอบแล้ว)
- ข้อมูลบริษัทใน `organizations` มี 1 แถว สร้างจาก "ขั้นที่ 1" ท้ายไฟล์ 006 ตามขั้นตอนปกติ

#### ทำไมแถว profiles ของแอดมินคนแรกไม่ได้เกิดจาก trigger (อ่านก่อนสงสัยตอนดูข้อมูลย้อนหลัง)
บัญชีอีเมลนี้ถูกสร้างไว้ใน `auth.users` **ก่อน** ที่ migration 006 จะถูกรัน จึงยังไม่มี trigger `on_auth_user_created` อยู่ในระบบตอนนั้น
และเนื่องจาก trigger ทำงานเฉพาะตอนมีการเพิ่มผู้ใช้ใหม่ (after insert) จึงไม่ย้อนกลับไปสร้าง profile ให้บัญชีที่มีอยู่ก่อนหน้า

ผู้ใช้จึงเลือก **insert แถวใน `profiles` ด้วยมือให้บัญชีเดิม** แทนการทำตาม "ขั้นที่ 3" ท้ายไฟล์ 006 ที่แนะนำให้กด Add user สร้างบัญชีใหม่
เหตุผล: ถ้าสร้างใหม่จะกลายเป็นมีบัญชีซ้ำสองอันโดยไม่จำเป็น ซึ่งสร้างความสับสนภายหลัง

คำสั่งที่ใช้ (บันทึกไว้เพื่อการตรวจสอบย้อนหลัง — insert แถวเดียว พร้อมตั้ง role เป็น admin และผูกเข้ากับบริษัทแถวแรก):
```sql
insert into public.profiles (id, org_id, full_name, role, is_active)
select
  u.id,
  (select id from public.organizations order by created_at limit 1),
  split_part(u.email, '@', 1),
  'admin',
  true
from auth.users u
where u.id = '1896c817-eb78-4549-bd55-17fe77cec197'
on conflict (id) do update
  set org_id = excluded.org_id,
      role = 'admin',
      is_active = true;
```

**สรุปสำหรับอนาคต:** ผู้ใช้คนอื่น ๆ ที่ถูกเพิ่มหลังจากนี้ จะได้แถว `profiles` อัตโนมัติจาก trigger ตามปกติ มีเพียงแอดมินคนแรกคนเดียวเท่านั้นที่เป็นข้อยกเว้น

**งานถัดไป:** Phase 3 — หน้าตั้งค่าองค์กร `/settings`

### 2026-08-20 — Phase 3: หน้าตั้งค่าองค์กร
**คำถามที่ลูกค้ายืนยันแล้ว (ปิดเครื่องหมาย ❓ ใน Spec)**
- เก็บช่องอัปโหลดลายเซ็นไว้ ลูกค้าต้องการฟีเจอร์นี้ (อัปโหลดเองทีหลัง) ทำเป็น optional เหมือนโลโก้
- **ตัดโหมด "พิมพ์ทับแบบฟอร์มสำเร็จรูป" ออกทั้งหมด** ลูกค้าไม่มีแบบฟอร์มกระดาษเดิม จึงไม่เผื่อคอลัมน์ตำแหน่ง X/Y ในฐานข้อมูลและไม่ทำ UI ส่วนนั้น เหลือโหมดพิมพ์ลงกระดาษเปล่าอย่างเดียว (Spec ข้อ 5 ข้อย่อยที่ 2 ถือว่ายกเลิก)
- ไม่ทำหน้าจัดการผู้ใช้ในรอบนี้ ยกไปเป็น Phase 3.5

**สิ่งที่ทำ**
- `supabase/migrations/007_storage_bucket.sql` — เพิ่มคอลัมน์ `doc_prefix` (ค่าตั้งต้น WHT), สร้างที่เก็บไฟล์ `org-assets` แบบไม่เปิดสาธารณะ จำกัด 2 MB รับเฉพาะ PNG และสร้าง policy 4 ข้อบน `storage.objects`
- `src/lib/org.js` — อ่าน/เขียนข้อมูลองค์กร อัปโหลด/ลบไฟล์ และขอลิงก์ชั่วคราวสำหรับแสดงรูป
- `src/lib/validate.js` — ตรวจเลขผู้เสียภาษี 13 หลัก และคำนำหน้าเลขที่เอกสาร ให้ตรงกับ CONSTRAINT ในฐานข้อมูล
- `src/components/asset-upload.js` — ชิ้นส่วนอัปโหลดรูป ใช้ซ้ำทั้งโลโก้และลายเซ็น
- `src/pages/settings.js` + `src/styles/settings.css` — หน้าตั้งค่า 3 ส่วน
- เพิ่มเส้นทาง `/settings` และลิงก์เมนูในแถบหัวเว็บ

**บันทึกการตัดสินใจ**
- ที่เก็บไฟล์ตั้งเป็นแบบ **ไม่เปิดสาธารณะ** เพราะโลโก้และลายเซ็นบริษัทไม่ควรให้ใครก็ได้บนอินเทอร์เน็ตเปิดดู เวลาแสดงรูปจึงขอลิงก์ชั่วคราวอายุ 1 ชั่วโมง (signed URL) เป็นครั้ง ๆ ไป
- คอลัมน์ `logo_url` / `signature_url` เก็บเป็น **ที่อยู่ไฟล์ภายใน** (เช่น `{org_id}/logo.png`) ไม่ใช่ URL เต็ม เพราะลิงก์เต็มมีวันหมดอายุ เก็บไว้ก็ใช้ไม่ได้
- แยกโฟลเดอร์เก็บไฟล์ตามรหัสองค์กร เพื่อให้เขียนกฎความปลอดภัยเทียบกับ `get_my_org_id()` ได้ตรงไปตรงมา
- ดูไฟล์ได้ทุกคนในองค์กร แต่อัปโหลด/ลบได้เฉพาะ admin เพราะหน้าพิมพ์เอกสารต้องแสดงโลโก้ให้ผู้ใช้ทั่วไปเห็นด้วย

**ผลการทดสอบเท่าที่ทำได้ (20 ส.ค. 2026)**
- `npm run build` ผ่าน ไม่มี error
- เปิด `/settings` ตอนไม่ได้ล็อกอิน เด้งไป `/login` อัตโนมัติ (ตัวกันหน้าครอบคลุมหน้าใหม่โดยอัตโนมัติตามที่ออกแบบไว้)
- Console ไม่มีข้อความสีแดง
- **ยังทดสอบไม่ได้:** การบันทึกข้อมูลบริษัท อัปโหลดโลโก้ และโหมดอ่านอย่างเดียวของ role `user` เพราะ (1) ยังไม่ได้รัน migration 007 จึงยังไม่มีคอลัมน์ `doc_prefix` และยังไม่มีที่เก็บไฟล์ (2) ยังไม่มีบัญชี role `user` ในระบบ

**งานถัดไป:** ผู้ใช้รัน 007 แล้วทดสอบหน้าตั้งค่าจริง จากนั้นเข้า Phase 3.5 (จัดการผู้ใช้) หรือข้ามไป Phase 4 ตามที่ตัดสินใจ

### 2026-08-20 — Phase 3 ปิดงาน (เจ้าของงานทดสอบจริงผ่านครบ)
เจ้าของงานรัน `007_storage_bucket.sql` บน Supabase และทดสอบหน้า `/settings` จริงบนเบราว์เซอร์ครบทุกข้อ ผลคือผ่านทั้งหมด:
- บันทึกข้อมูลบริษัท (ชื่อ / ที่อยู่ / ผู้ลงนาม) ขึ้นแถบเขียว กดรีเฟรชแล้วข้อมูลยังอยู่ครบ
- กรอกเลขประจำตัวผู้เสียภาษีผิดเป็น 12 หลัก ขึ้นแถบแดงแจ้งเตือนถูกต้อง
- อัปโหลดและลบโลโก้ ทำงานถูกต้องทั้งสองอย่าง
- เปลี่ยนคำนำหน้าเลขที่เอกสาร ตัวอย่างเปลี่ยนตามที่พิมพ์แบบเรียลไทม์ และบันทึกได้

### 2026-08-20 — Phase 3.5: หน้าจัดการผู้ใช้
**สิ่งที่ทำ**
- `src/lib/users.js` — ดึงรายชื่อผู้ใช้ในองค์กร แก้บทบาท/สถานะ และด่านตรวจกันไม่ให้เหลือผู้ดูแลระบบศูนย์คน
- `src/components/user-manager.js` — ตารางรายชื่อผู้ใช้ พร้อมช่องเปลี่ยนบทบาทและปุ่มเปิด-ปิดการใช้งาน มีข้อความยืนยันก่อนบันทึกทุกครั้ง และมีคำอธิบายขั้นตอนเพิ่มผู้ใช้ใหม่ผ่าน Supabase Dashboard
- แยกไฟล์ `src/pages/settings.js` ที่ยาว 366 บรรทัด (เกินเพดาน ~300 บรรทัดตาม CLAUDE.md ข้อ 5) ออกเป็น `components/org-form.js`, `components/doc-number-form.js` และย้ายตัวช่วยที่ใช้ร่วมกันไป `lib/ui.js` ตอนนี้ทุกไฟล์ในโปรเจกต์ยาวไม่เกิน 200 บรรทัด

**ผลการตรวจ RLS ของตาราง profiles (ข้อ 6 ที่ได้รับมอบหมาย)**
ตรวจแล้วพบว่า **policy เดิมในไฟล์ 004 รองรับอยู่แล้ว ไม่ต้องเขียน migration 008 เพิ่ม**
policy `profiles_update` เขียนไว้ว่า `using (org_id = get_my_org_id() and get_my_role() = 'admin')` พร้อม `with check` เงื่อนไขเดียวกัน
แปลว่าผู้ดูแลระบบแก้ไขแถวของใครก็ได้ที่อยู่ในองค์กรเดียวกัน รวมถึงคอลัมน์ `role` และ `is_active` ส่วนผู้ใช้ทั่วไปจะถูกปฏิเสธทุกกรณี

**บันทึกการตัดสินใจ**
- ตารางผู้ใช้แสดง "ชื่อ" ไม่แสดง "อีเมล" เพราะอีเมลอยู่ในตาราง `auth.users` ซึ่ง Supabase ไม่เปิดให้ฝั่งเว็บอ่านด้วยเหตุผลด้านความปลอดภัย ค่าตั้งต้นของชื่อคือส่วนหน้าเครื่องหมาย @ ของอีเมลอยู่แล้ว จึงยังพอแยกคนออกจากกันได้
- ด่านกันผู้ดูแลระบบคนสุดท้าย อ่านรายชื่อใหม่จากฐานข้อมูลทุกครั้งก่อนตัดสินใจ เผื่อกรณีมีผู้ดูแลอีกคนแก้ไขพร้อมกันอยู่ ไม่ใช้ข้อมูลที่ค้างอยู่บนหน้าจอ
- ด่านนี้ทำงานอยู่ฝั่งหน้าเว็บเท่านั้น ยังไม่ได้ทำเป็น trigger ในฐานข้อมูล ถ้าต้องการกันให้แน่นหนากว่านี้สามารถเพิ่มภายหลังได้ (กรณีเลวร้ายสุดแก้ได้ด้วยการรัน SQL คืนสิทธิ์ใน SQL Editor)

**ผลการทดสอบเท่าที่ทำได้**
- `npm run build` ผ่าน ไม่มี error
- เปิด `/settings` ตอนไม่ได้ล็อกอิน เด้งไป `/login` ถูกต้อง
- Console ไม่มีข้อความสีแดง
- **ยังทดสอบไม่ได้:** การเปลี่ยนบทบาทและเปิด-ปิดการใช้งานจริง รวมถึงการตรวจว่าผู้ใช้ทั่วไปไม่เห็นหัวข้อจัดการผู้ใช้ ต้องมีบัญชีทดสอบคนที่สองในระบบก่อน

**งานถัดไป:** เจ้าของงานทดสอบ Phase 3.5 ให้ผ่านก่อน แล้วค่อยเริ่ม Phase 4 (ทะเบียนผู้ถูกหักภาษี)

### 2026-08-20 — Phase 3.5 ปิดงาน (เจ้าของงานทดสอบจริงผ่านครบ)
เจ้าของงานทดสอบหน้าจัดการผู้ใช้จริงบนเบราว์เซอร์ครบทุกข้อ ผลผ่านทั้งหมด:
- ตารางรายชื่อผู้ใช้แสดงถูกต้อง เปลี่ยนบทบาทและเปิด-ปิดการใช้งานได้ พร้อมกล่องยืนยันก่อนบันทึกทุกครั้ง
- **ด่านกันล็อกตัวเองออกจากระบบทำงานจริง** เมื่อลองลดสิทธิ์ตัวเองขณะเป็นผู้ดูแลระบบคนเดียวที่เหลืออยู่ ระบบบล็อกและขึ้นแถบแดงแจ้งเตือนถูกต้อง ไม่ยอมให้บันทึก
- **ผู้ใช้ทั่วไปมองไม่เห็นหัวข้อจัดการผู้ใช้เลย** และช่องกรอกอื่นในหน้าตั้งค่าเป็นแบบอ่านอย่างเดียวตามที่ออกแบบไว้

ไม่ต้องรัน migration เพิ่มในเฟสนี้ เพราะ policy `profiles_update` ในไฟล์ 004 รองรับการที่ผู้ดูแลระบบแก้ไข `role` และ `is_active` ของผู้ใช้คนอื่นในองค์กรเดียวกันอยู่แล้ว

### 2026-08-20 — Phase 4: ทะเบียนผู้ถูกหักภาษี
**สิ่งที่ทำ**
- `supabase/migrations/008_payee_usage_stats.sql` — มุมมอง (view) สรุป "จำนวนเอกสารที่เคยออก" และ "ใช้ล่าสุดเมื่อ" ของผู้ถูกหักภาษีแต่ละราย นับจากตาราง `wht_certificates` จริง
- `src/lib/payees.js` — ค้นหาแบบแบ่งหน้า ดึงสถิติ ตรวจเลขซ้ำ เพิ่ม แก้ไข และปิด/เปิดการใช้งาน
- `src/lib/format.js` — แปลงวันที่เป็นแบบไทย พ.ศ. (ส่วนจัดรูปแบบจำนวนเงินจะเพิ่มใน Phase 5)
- `src/components/modal.js` — กล่องหน้าต่างซ้อนแบบใช้ซ้ำได้ ใช้แท็ก `<dialog>` ของเบราว์เซอร์
- `src/components/payee-form.js` — ฟอร์มเพิ่ม/แก้ไข ครบทุกช่องตาม Spec ตาราง `payees`
- `src/components/payee-table.js` — ตารางรายชื่อ 6 คอลัมน์
- `src/pages/payees.js` + `src/styles/payees.css` — หน้า `/payees` พร้อมค้นหาแบบหน่วง 300 มิลลิวินาที และแบ่งหน้า 20 รายการ
- เพิ่มเส้นทาง `/payees` และลิงก์ในแถบหัวเว็บ

**บันทึกการตัดสินใจ**
- **สถิติการใช้งานใช้ view ในฐานข้อมูล ไม่นับที่ฝั่งเว็บ** เพราะถ้าให้หน้าเว็บโหลดเอกสารทั้งหมดมานับเองจะช้ามากเมื่อข้อมูลเยอะ (ผู้ถูกหัก 20 รายที่มีเอกสารรายละ 100 ใบ = ต้องโหลด 2,000 แถว)
- view ตั้ง `security_invoker = true` เพื่อให้ทำงานด้วยสิทธิ์ของผู้เรียกดู กฎ RLS จึงยังทำงานตามปกติ ถ้าไม่ตั้งค่านี้ผู้ใช้จะเห็นสถิติขององค์กรอื่นได้
- **นับเฉพาะเอกสารที่ออกไปแล้วจริง** ใบร่าง (draft) ไม่นับเพราะยังไม่ได้ออกให้ใคร ส่วนเอกสารที่ยกเลิก (void) ยังนับ เพราะเคยออกไปจริงแล้วและถือเป็นประวัติการใช้งาน
- **ปิดใช้งานเท่านั้น ห้ามลบจริง** ตาม CLAUDE.md ข้อ 4.7 เพราะเอกสารภาษีที่ออกไปแล้วอ้างอิงรายชื่อนี้อยู่
- ตรวจเลขซ้ำที่ฝั่งเว็บก่อนบันทึกเสมอ แล้วแสดงชื่อรายการเดิมพร้อมปุ่มกดไปเปิดดูได้ทันที แทนที่จะปล่อยให้ฐานข้อมูลปฏิเสธด้วยข้อความภาษาอังกฤษ ส่วน unique constraint ในฐานข้อมูลยังทำหน้าที่เป็นด่านสุดท้าย
- เพิ่มช่องติ๊ก "แสดงรายชื่อที่ปิดใช้งานด้วย" เพราะไม่งั้นรายชื่อที่ปิดไปแล้วจะหาไม่เจออีกเลย
- ตัดอักขระพิเศษ (จุลภาค วงเล็บ เปอร์เซ็นต์) ออกจากคำค้นก่อนส่งไปฐานข้อมูล เพราะอักขระเหล่านี้มีความหมายพิเศษในเงื่อนไขค้นหาของ Supabase ถ้าไม่ตัดจะค้นไม่เจอโดยไม่มีสาเหตุที่ผู้ใช้เข้าใจได้
- **ยังไม่ทำ "นำเข้าจาก Excel"** ตามที่ตกลงกันว่ายกไปเฟสหลัง

**ผลการทดสอบเท่าที่ทำได้**
- `npm run build` ผ่าน ไม่มี error
- เปิด `/payees` ตอนไม่ได้ล็อกอิน เด้งไป `/login` ถูกต้อง
- Console ไม่มีข้อความสีแดง
- ไฟล์ที่ยาวที่สุดในโปรเจกต์คือ 236 บรรทัด ยังต่ำกว่าเพดาน ~300 บรรทัดตามกฎ CLAUDE.md ข้อ 5
- **ยังทดสอบไม่ได้:** การเพิ่ม/แก้ไข/ค้นหา/แบ่งหน้า/เตือนเลขซ้ำจริง เพราะต้องล็อกอินและต้องรัน migration 008 ก่อน

**งานถัดไป:** เจ้าของงานรัน 008 แล้วทดสอบหน้า `/payees` ให้ผ่านก่อน จึงเริ่ม Phase 5 (ตรรกะคำนวณ)

### 2026-08-20 — Phase 4 ปิดงาน (เจ้าของงานทดสอบจริงผ่านครบ)
เจ้าของงานรัน `008_payee_usage_stats.sql` บน Supabase ผ่านครบทั้ง 3 ข้อตรวจสอบ และทดสอบหน้า `/payees` จริงบนเบราว์เซอร์ผ่านครบทุกข้อ:
- เพิ่มรายชื่อผู้ถูกหักภาษีได้ทั้งแบบบุคคลธรรมดาและนิติบุคคล
- **คำเตือนเลขประจำตัวผู้เสียภาษีซ้ำทำงานจริง** ขึ้นชื่อรายการเดิม และปุ่ม "เปิดรายการเดิม" พาไปแก้รายการนั้นได้ถูกต้อง
- กรอกเลขผู้เสียภาษีไม่ครบ 13 หลัก ขึ้นข้อความแจ้งเตือนถูกต้อง
- ค้นหาด้วยชื่อและด้วยเลขผู้เสียภาษี ใช้งานได้ทั้งสองแบบ
- ปิดและเปิดการใช้งานรายชื่อทำงานถูกต้อง ข้อมูลไม่ถูกลบจริง
- ย่อหน้าจอเท่ามือถือ ตารางเปลี่ยนเป็นการ์ดเรียงลง ไม่ล้นจอ

**เพิ่มเติมในบันทึกการตัดสินใจของ Phase 4 (เจ้าของงานตัดสินใจแล้ว)**
- **การนับ "จำนวนเอกสารที่เคยออก" ใช้ตามที่ออกแบบไว้เดิม** คือนับเฉพาะเอกสารสถานะ `issued` และ `void` ไม่นับใบร่าง (`draft`) — ยืนยันแล้วว่าไม่ต้องแก้ไฟล์ 008
- **อนุมัติให้ติดตั้ง Vitest เป็นตัวรันเทสต์ของโปรเจกต์** (เป็นการอนุมัติติดตั้งไลบรารีใหม่ตามกฎ CLAUDE.md ข้อ 3 และข้อ 8)

### 2026-08-20 — Phase 5: ตรรกะคำนวณ (ทดสอบเองผ่านครบ 60 เคส)
**สิ่งที่ทำ**
- ติดตั้ง **Vitest** เป็นตัวรันเทสต์ (ได้รับอนุมัติจากเจ้าของงานแล้ว) เพิ่มคำสั่ง `npm run test` และ `npm run test:watch` ใน package.json
- `src/lib/tax.js` — คำนวณภาษีต่อบรรทัด รวมยอดทั้งเอกสาร และตัวปัดเศษแบบ half-up
- `src/lib/bahtText.js` — แปลงจำนวนเงินเป็นตัวอักษรไทย รองรับหลักล้านซ้อนไม่จำกัดชั้น
- `src/lib/format.js` — เพิ่มการจัดรูปแบบจำนวนเงิน และการแสดงวันที่แบบเต็มสำหรับหน้าพิมพ์
- ไฟล์ทดสอบ 3 ไฟล์คู่กับไฟล์โค้ด รวม 60 เคส ผ่านทั้งหมด

**ผลการทดสอบ (รันเองด้วย `npm run test`)**
- `bahtText.test.js` 24 เคส ผ่านหมด (ครอบคลุมทุกแถวในตาราง Spec ข้อ 3.3 ครบ 9 แถว)
- `tax.test.js` 24 เคส ผ่านหมด
- `format.test.js` 12 เคส ผ่านหมด
- `npm run build` ยังผ่านปกติ และตรวจแล้วว่าไฟล์ทดสอบไม่ถูกรวมเข้าไฟล์ที่ส่งขึ้นเว็บจริง

**บันทึกการตัดสินใจ**
- **ปัญหาการปัดเศษที่พบและแก้แล้ว:** วิธีที่คนมักเขียนกันคือ `Math.round(x * 100) / 100` ให้ผลผิดกับเงินบางจำนวน เพราะคอมพิวเตอร์เก็บทศนิยมเป็นเลขฐานสองจึงเก็บได้ไม่ตรงเป๊ะ ทดสอบแล้วพบเลขที่ทำพลาดจริง 3 ตัวคือ **1.005, 0.145 และ 10.075** ซึ่งวิธีธรรมดาจะปัดลง ทำให้ยอดขาดไป 1 สตางค์
- **วิธีแก้ที่เลือกใช้:** เลื่อนจุดทศนิยมผ่านข้อความ (เขียนเป็น `"1.005e2"` แล้วให้ตัวแปลงตัวเลขอ่าน ได้ 100.5 ตรงเป๊ะ) แทนการคูณด้วย 100 **ไม่ต้องติดตั้งไลบรารีคำนวณทศนิยมเพิ่ม** จึงไม่ต้องขออนุมัติเพิ่มตามกฎ CLAUDE.md ข้อ 3
- ตัวปัดเศษตัวเดียวกันนี้ถูกใช้ร่วมกันทั้ง 3 ไฟล์ เพื่อให้ยอดที่แสดงบนหน้าจอ ยอดที่อ่านเป็นตัวอักษร และยอดที่บันทึกลงฐานข้อมูล ตรงกันเสมอ
- **กฎ "เอ็ด" ทำตามสเปกอย่างเคร่งครัด** คือใช้เมื่อหลักหน่วยเป็น 1 และมีหลักสิบนำหน้าเท่านั้น ดังนั้น 101 อ่านว่า "หนึ่งร้อยหนึ่ง" ตามตัวอย่างในสเปก (ไม่ใช่ "หนึ่งร้อยเอ็ด" ตามที่บางตำราใช้) หากลูกค้าต้องการแบบหลัง ต้องแก้เงื่อนไขเดียวใน bahtText.js
- **แก้ข้อบกพร่องที่พบระหว่างทาง:** ฟังก์ชันแสดงวันที่ที่เขียนไว้ตอน Phase 4 ส่งข้อความวันที่ให้ตัวแปลงของเบราว์เซอร์จัดการเอง ซึ่งจะถือว่าเป็นเวลามาตรฐานโลกแล้วแปลงกลับเป็นเวลาท้องถิ่น ทำให้เครื่องที่ตั้งโซนเวลาก่อนหน้า UTC แสดงวันที่ย้อนไป 1 วัน แก้เป็นอ่านตัวเลขจากข้อความตรง ๆ แล้ว พร้อมเคสทดสอบกำกับไว้
- `bahtText` คืน **ข้อความว่าง** เมื่อได้รับค่าที่ไม่ใช่ตัวเลข ไม่คืน "ศูนย์บาทถ้วน" เพราะบนเอกสารภาษี การแสดงยอดศูนย์ทั้งที่ข้อมูลผิดพลาด อันตรายกว่าการเว้นว่างไว้ให้เห็นชัดว่ามีปัญหา

**คำถามที่ยังค้างในสเปก (ต้องตอบก่อน Phase 8 หน้าพิมพ์)**
- Spec ข้อ 3.3 มีเครื่องหมาย ❓ ค้างอยู่ว่า **ตัวเลขในเอกสารต้องเป็นเลขไทย (๑๒๓) หรือเลขอารบิก (123)** ตอนนี้ระบบใช้เลขอารบิกตามค่าตั้งต้น ยังไม่ได้ยืนยันกับลูกค้า

**งานถัดไป:** เจ้าของงานตรวจผลทดสอบ Phase 5 แล้วคุยขอบเขต Phase 6 (ฟอร์มสร้างเอกสาร 50 ทวิ) ก่อนเริ่มเขียน

### 2026-08-20 — แก้กฎการอ่าน "เอ็ด" และปิดคำถามค้างในสเปก
- เจ้าของงานยืนยันว่ากฎ "เอ็ด" ให้ใช้ตามหลักภาษาไทยมาตรฐาน คือหลักหน่วยที่เป็น 1 อ่านว่า "เอ็ด" ทุกครั้งที่จำนวนนั้นมีมากกว่า 1 หลัก ไม่ใช่เฉพาะตอนหลักสิบไม่เป็นศูนย์
- แก้ `src/lib/bahtText.js` ให้ถูกต้องตามกฎนี้ และเพิ่มเคสทดสอบครอบคลุมทุกตำแหน่งหลัก (101, 201, 1001, 10001, 100001, 1000101) รวมทั้งเคสหลักล้านซ้อน (1000000000001) ตอนนี้เทสต์ทั้งโปรเจกต์มี 61 เคส ผ่านหมด
- **แก้จุดพิมพ์ผิดใน `Spec.md` ข้อ 3.3** จาก 101 = "หนึ่งร้อยหนึ่งบาทถ้วน" เป็น "หนึ่งร้อยเอ็ดบาทถ้วน" และเขียนกฎย่อยใหม่ให้ชัด
- **ปิดคำถามค้าง ❓ ใน `Spec.md` ข้อ 6.1** เจ้าของงานยืนยันว่าใบร่างไม่กินเลขที่เอกสารจริง เลขจะออกเมื่อกด "บันทึกและออกเอกสาร" เท่านั้น
- หมายเหตุ: การอ่านสตางค์ยังใช้ "หนึ่ง" เมื่อเป็นเลขหลักเดียว (1 สตางค์ = "หนึ่งสตางค์") เพราะเป็นเลขโดด ๆ ไม่เข้าเงื่อนไข "มากกว่า 1 หลัก"

### 2026-08-20 — Phase 6: ฟอร์มสร้างเอกสาร 50 ทวิ
**สิ่งที่ทำ**
- `supabase/migrations/009_issue_doc_no.sql` — ฟังก์ชันฐานข้อมูลออกเลขที่เอกสารแบบกันเลขชนกัน
- `src/lib/certificates.js` — บันทึกใบร่าง บันทึกและออกเอกสาร พร้อมบันทึก `audit_logs`
- `src/components/payee-autocomplete.js` — ช่องเลือกผู้ถูกหักภาษีแบบพิมพ์ไปค้นไป พร้อมปุ่มเพิ่มรายชื่อใหม่
- `src/components/income-lines.js` — ตารางรายการเงินได้ คำนวณสด รองรับการแก้ยอดภาษีเอง และกด Enter เพิ่มบรรทัด
- `src/components/certificate-meta.js` และ `certificate-totals.js` — ส่วนหัวและส่วนท้ายของฟอร์ม
- `src/pages/new-certificate.js` + `src/styles/new-certificate.css` — หน้า `/new`
- เพิ่มเส้นทาง `/new` และเมนูในแถบหัวเว็บ

**บันทึกการตัดสินใจ**
- **ลำดับการบันทึกออกแบบให้เลขที่เอกสารไม่ขาดช่วง** คือบันทึกเป็นใบร่างก่อน แล้วบันทึกรายการเงินได้ เมื่อทุกอย่างเรียบร้อยจึงค่อยขอเลขที่เอกสารเป็นขั้นสุดท้าย ถ้าพังกลางคันเอกสารจะค้างเป็นใบร่างและยังไม่มีเลขไหนถูกใช้ทิ้ง
- **หลังบันทึกสำเร็จ เลือกให้อยู่หน้าเดิมแล้วล้างฟอร์ม** ไม่พากลับหน้าแรก เพราะงานฝ่ายบัญชีมักออกเอกสารต่อกันหลายใบรวดในคราวเดียว การเด้งออกไปหน้าอื่นแล้วต้องกดกลับเข้ามาใหม่ทุกใบจะเสียเวลามาก (เมื่อทำหน้าประวัติเสร็จใน Phase 7 ค่อยเพิ่มลิงก์ "ดูเอกสารที่เพิ่งออก" ในข้อความแจ้งผลได้)
- **ถ้าผู้ใช้พิมพ์เลขที่เอกสารเอง ระบบจะใช้เลขนั้นและไม่ไปกินเลขจากตัวนับ** เพื่อไม่ให้ลำดับอัตโนมัติเลื่อนโดยไม่จำเป็น และมีข้อความบอกชัดเมื่อเลขที่พิมพ์เองซ้ำกับใบที่ออกไปแล้ว
- **ช่อง "ลำดับที่" ทำเป็นช่องอ่านอย่างเดียว** แสดงเลข 4 หลักท้ายที่ดึงจากเลขที่เอกสาร เพราะโครงสร้างตาราง `wht_certificates` ในสเปกมีแค่ `book_no` และ `doc_no` ไม่มีคอลัมน์สำหรับลำดับที่แยกต่างหาก (ดูหัวข้อคำถามค้างด้านล่าง)
- ค่าตั้งต้นของ "แบบที่ยื่นรายการ" เลือกเป็น ภ.ง.ด.3 เพราะเป็นแบบที่ใช้กับผู้รับเงินที่เป็นบุคคลธรรมดา ซึ่งพบบ่อยที่สุด

**คำถามช่อง "ลำดับที่" — เจ้าของงานอนุมัติแล้ว (20 ส.ค. 2026)**
สเปกข้อ 3.1 ระบุ "ลำดับที่ / เล่มที่ / เลขที่" แต่โครงสร้างตารางในสเปกข้อ 1.1 มีเพียง `book_no` และ `doc_no`
**เจ้าของงานอนุมัติให้ใช้แนวทางที่เสนอไว้** คือตีความ "ลำดับที่" เป็นเลข 4 หลักท้ายของเลขที่เอกสาร แสดงเป็นช่องอ่านอย่างเดียว
**ไม่เพิ่มคอลัมน์ใหม่ในตาราง** โครงสร้างฐานข้อมูลจึงคงเดิมทุกประการ ถือว่าคำถามข้อนี้ปิดแล้ว

**ผลการทดสอบเท่าที่ทำได้**
- `npm run build` ผ่าน และ `npm run test` ผ่านครบ 61 เคส
- เปิด `/new` ตอนไม่ได้ล็อกอิน เด้งไป `/login` ถูกต้อง Console ไม่มีข้อความสีแดง
- **ยังทดสอบไม่ได้ทั้งหมด:** ทุกอย่างที่ต้องล็อกอิน ตั้งแต่การค้นหาผู้ถูกหักภาษี การคำนวณสด การกด Enter เพิ่มบรรทัด ไปจนถึงการบันทึกร่างและออกเอกสารจริง

**งานถัดไป:** เจ้าของงานรัน 009 แล้วทดสอบหน้า `/new` ให้ผ่านก่อน จึงเริ่ม Phase 7 (ประวัติเอกสาร)

### 2026-08-20 — แก้บั๊กเปิดหน้า /new ไม่ได้ (Cannot access 'totals' before initialization)

**อาการ:** เปิดหน้าสร้างเอกสารแล้วขึ้น "เปิดหน้านี้ไม่สำเร็จ" เต็มจอ ใช้งานไม่ได้เลย

**สาเหตุจริง (ไม่ใช่ circular import):**
หน้า `/new` สร้างชิ้นส่วนตามลำดับ โดยสร้างตารางรายการเงินได้ก่อน แล้วจึงสร้างส่วนยอดรวมทีหลัง
แต่ตัวตารางรายการเงินได้กลับ "แจ้งยอดกลับไปให้หน้าจอหลัก" ตั้งแต่ตอนกำลังสร้างตัวเองยังไม่เสร็จ
หน้าจอหลักจึงถูกสั่งให้ไปอัปเดตส่วนยอดรวม ทั้งที่ตัวแปรของส่วนยอดรวมยังไม่ถูกสร้างขึ้นมา
JavaScript จึงหยุดทำงานทันทีด้วยข้อผิดพลาด Cannot access 'totals' before initialization

ตรวจแล้วยืนยันว่า **ไม่ได้เกิดจากวงจรการ import (circular import)** ตามที่สงสัยไว้เป็นทางเลือกที่สอง

**วิธีแก้ที่ต้นเหตุ:**
แยกการทำงานของตารางรายการเงินได้ออกเป็น 2 อย่างชัดเจน
- `paint()` วาดตารางอย่างเดียว ไม่แจ้งใคร — ใช้ตอนกำลังสร้างชิ้นส่วน
- `render()` วาดแล้วแจ้งยอดกลับ — ใช้เฉพาะตอนผู้ใช้ทำอะไรจริง ๆ ซึ่งตอนนั้นหน้าจอสร้างเสร็จหมดแล้ว

หลักการที่ยึดต่อจากนี้: **ชิ้นส่วนต้องไม่ยิง callback กลับไปหาผู้เรียก ตั้งแต่ตอนที่ตัวเองยังสร้างไม่เสร็จ**
เพราะผู้เรียกยังเตรียมตัวไม่พร้อม หน้าที่แจ้งยอดครั้งแรกเป็นของผู้เรียกซึ่งจะเรียกเองเมื่อพร้อมแล้ว
วิธีนี้แก้ที่รากของปัญหา ไม่ใช่แค่สลับลำดับการประกาศตัวแปรให้รอดไปครั้งเดียว

**วิธีตรวจสอบที่ใช้ก่อนส่งงาน (เพิ่มขั้นตอนจากเดิม):**
1. `npm run test` ผ่านครบ 61 เคส และ `npm run build` ผ่าน — ตรวจไวยากรณ์และการ import ทั้งหมด
2. เขียนสคริปต์ตรวจเพิ่ม 2 อย่างที่ build มองไม่เห็น รันแล้วผ่านทั้งคู่
   - ไล่กราฟการ import ทุกไฟล์ใน `src/` หาวงจรการ import — **ไม่พบวงจรเลย**
   - ไล่หาชิ้นส่วนที่เรียก callback ของผู้เรียกตั้งแต่ตอนกำลังสร้างตัวเอง — **ไม่พบแล้ว**
3. ตรวจลำดับด้วยตาอีกครั้งว่าหน้า `/new` แจ้งยอดครั้งแรกหลังสร้างส่วนยอดรวมเสร็จแล้วจริง

บทเรียน: `npm run build` และ `npm run test` ผ่าน ไม่ได้แปลว่าหน้าจอเปิดได้
เพราะสองอย่างนี้ตรวจได้แค่ไวยากรณ์และการเชื่อมไฟล์ ไม่ได้ลองรันโค้ดในหน้าจริง

### 2026-08-20 — Phase 6 ปิดงาน (เจ้าของงานทดสอบจริงผ่านครบ)
เจ้าของงานรัน `009_issue_doc_no.sql` และทดสอบหน้า `/new` จริงบนเบราว์เซอร์ผ่านครบทุกข้อ:
- ค้นหาผู้ถูกหักภาษีแบบพิมพ์ไปค้นไป และเพิ่มรายชื่อใหม่กลางฟอร์มได้โดยข้อมูลที่กรอกค้างไว้ไม่หาย
- คำนวณภาษีสดขณะพิมพ์ ยอดรวมและตัวอักษรไทยเปลี่ยนตามทันที
- กด Enter ที่บรรทัดสุดท้ายเพิ่มบรรทัดใหม่ได้
- แก้ยอดภาษีเองแล้วระบบหยุดคำนวณทับ และกดคำนวณใหม่กลับมาคำนวณอัตโนมัติได้
- **บันทึกร่างไม่กินเลขที่เอกสาร** ตรวจแล้วเลขตัวอย่างยังเป็นเลขเดิม
- **ออกเอกสารจริงกินเลข 1 เลข และเลขถัดไปขยับถูกต้อง**
- การตรวจความครบถ้วนของข้อมูลทำงานครบ และเลือกผู้จ่ายเงินเป็น "อื่น ๆ" มีช่องกรอกเพิ่มให้
- ตรวจข้อมูลใน Supabase ครบถ้วน รวมถึง `payee_snapshot` และ `payer_snapshot`

**บั๊กที่เจอและแก้ระหว่างเฟสนี้:** เปิดหน้า `/new` ไม่ได้เลย ขึ้น "เปิดหน้านี้ไม่สำเร็จ" เต็มจอ
สาเหตุคือตารางรายการเงินได้แจ้งยอดกลับไปหาหน้าจอหลักตั้งแต่ตอนที่ตัวเองยังสร้างไม่เสร็จ
หน้าจอหลักจึงถูกสั่งให้อัปเดตส่วนยอดรวมทั้งที่ยังไม่ได้สร้างส่วนนั้นขึ้นมา (ไม่ได้เกิดจาก circular import ตามที่สงสัยไว้)
แก้โดยแยก `paint()` (วาดอย่างเดียว ใช้ตอนสร้างชิ้นส่วน) ออกจาก `render()` (วาดแล้วแจ้งกลับ ใช้ตอนผู้ใช้ทำอะไร)

### 2026-08-20 — Phase 7: ประวัติเอกสาร

**ช่องโหว่ความปลอดภัยที่ตรวจพบและปิดแล้ว (สำคัญที่สุดของรอบนี้)**
ตรวจ policy เดิมของ `wht_certificates` ในไฟล์ 004 พบว่าเขียนไว้เพียง
`using (org_id = get_my_org_id()) with check (org_id = get_my_org_id())`
ซึ่งแปลว่า **ใครก็ได้ในองค์กร แก้เอกสารใบไหนก็ได้ รวมถึงใบที่ออกไปแล้ว** ขัดกับกฎ 2 ข้อ
- Spec.md ข้อ 4 และ 6.1: เอกสารที่ออกแล้วห้ามแก้ตัวเลขใด ๆ ถ้าผิดต้องยกเลิกแล้วออกใบใหม่
- Spec.md ข้อ 0: การยกเลิกเอกสารทำได้เฉพาะ admin

แก้ด้วย `010_protect_issued_certificates.sql` ซึ่งปิดช่องโหว่ 2 ชั้น
- **ชั้น RLS** แยก policy การแก้ไขเป็น 2 ข้อ คือ "แก้ใบร่าง" (ทุกคนในองค์กร) กับ "ยกเลิกเอกสารที่ออกแล้ว" (เฉพาะ admin และผลลัพธ์ต้องเป็นสถานะยกเลิกเท่านั้น)
- **ชั้น trigger** คุมว่าเปลี่ยนค่าอะไรได้บ้าง เพราะ RLS บอกได้แค่ว่าแตะแถวไหนได้ แต่บอกไม่ได้ว่าห้ามแก้คอลัมน์ไหน ถ้ามีแต่ RLS ผู้ดูแลระบบจะยังแอบแก้ยอดเงินไปพร้อมกับการยกเลิกได้ในคำสั่งเดียว
- เพิ่ม trigger ล็อกตาราง `wht_certificate_items` ด้วย เพราะถ้าล็อกแต่หัวเอกสาร บรรทัดรายการข้างในยังถูกแก้หรือลบได้ ทำให้ยอดรวมไม่ตรงกับรายละเอียด ซึ่งเป็นสิ่งแรกที่สรรพากรจะเห็นเวลาตรวจ

**สิ่งที่ทำ**
- `src/lib/history.js` — ดึงรายการพร้อมตัวกรอง แถบสรุป ข้อมูลส่งออก และการยกเลิกเอกสาร
- `src/lib/csv.js` และ `src/lib/history-export.js` — สร้างและดาวน์โหลดไฟล์ CSV
- `src/lib/certificate-prefill.js` — เตรียมข้อมูลเดิมเติมกลับเข้าฟอร์ม สำหรับโหมดคัดลอกและแก้ไข
- `src/lib/doc-no.js` — แยกเรื่องเลขที่เอกสารออกจาก `certificates.js`
- `src/components/history-filters.js`, `history-table.js`, `void-form.js` — แถบตัวกรอง ตาราง และฟอร์มยกเลิก
- `src/pages/history.js` + `src/styles/history.css` — หน้า `/history`
- ต่อยอดฟอร์ม Phase 6 เดิมให้รับค่าตั้งต้นได้ (ไม่ได้สร้างฟอร์มใหม่) และเพิ่มคำสั่ง `updateDraft` กับ `issueExistingDraft`

**บันทึกการตัดสินใจ**
- **แถบสรุปนับจากทุกใบที่ผ่านตัวกรอง ไม่ใช่แค่หน้าที่กำลังดู** และใช้เงื่อนไขการกรองชุดเดียวกับตารางและการส่งออก เขียนไว้ที่ฟังก์ชันเดียว เพื่อไม่ให้เกิดกรณีตัวเลขในแถบสรุปไม่ตรงกับรายการที่เห็น
- **ปุ่มแก้ไขสร้างขึ้นเฉพาะแถวที่เป็นใบร่าง** ไม่ได้แค่ซ่อนด้วย CSS และมีด่านกันซ้อนอีก 3 ชั้น คือคำสั่งบันทึกระบุ `status = 'draft'` กำกับ, กฎ RLS, และ trigger ในฐานข้อมูล
- **โหมดคัดลอกและแก้ไขส่งผ่านที่อยู่เว็บ** (`/new?copy=<รหัส>` และ `/new?edit=<รหัส>`) ทำให้ผู้ใช้กดปุ่มย้อนกลับของเบราว์เซอร์ได้ตามปกติ และส่งลิงก์ต่อให้เพื่อนร่วมงานได้
- **คงเครื่องหมาย "แก้ด้วยมือ" ไว้ตอนเปิดเอกสารเดิม** โดยเทียบยอดภาษีที่บันทึกไว้กับที่คำนวณได้ ถ้าไม่ตรงแปลว่าผู้ใช้เคยพิมพ์เอง ไม่งั้นพอเปิดฟอร์มระบบจะคำนวณทับแล้วยอดเปลี่ยนเงียบ ๆ
- **ใส่เครื่องหมาย = นำหน้าเลขผู้เสียภาษีในไฟล์ CSV** เพื่อบังคับให้ Excel มองเป็นข้อความ ไม่งั้นเลข 13 หลักจะถูกแปลงเป็นตัวเลขวิทยาศาสตร์และเลขศูนย์นำหน้าหายไป
- **ไฟล์ CSV เข้ารหัสแบบ UTF-8 พร้อม BOM** ไม่งั้น Excel บนวินโดวส์จะอ่านตัวอักษรไทยเป็นตัวขยะทั้งไฟล์
- ปุ่ม "ดู" และ "พิมพ์ซ้ำ" แสดงไว้แต่กดไม่ได้ พร้อมข้อความบอกว่าจะเปิดใช้งานพร้อมหน้าพิมพ์ใน Phase 8
- **ยังไม่ทำไฟล์รูปแบบยื่น ภ.ง.ด.3 / ภ.ง.ด.53** ตามที่ตกลง (คำถามค้างข้อ 20 กลุ่ม E) ไฟล์ที่ส่งออกรอบนี้เป็นรายการสรุปธรรมดาสำหรับให้ผู้ทำบัญชีเอาไปกรองต่อ

**วิธีตรวจสอบที่ใช้ก่อนส่งงาน (ตามบทเรียนจากรอบก่อน)**
1. `npm run test` ผ่าน 61 เคส และ `npm run build` ผ่าน
2. รันสคริปต์ตรวจ 3 อย่างที่ build มองไม่เห็น ผ่านทั้งหมด
   - วงจรการ import — ไม่พบ
   - ชิ้นส่วนที่ยิง callback ตั้งแต่ตอนสร้างตัวเอง (ต้นเหตุบั๊กรอบก่อน) — ไม่พบ
   - ตัวแปรที่ถูกใช้ก่อนบรรทัดประกาศ — ไม่พบ
3. เปิด `/history` และ `/new?copy=...` ด้วยเบราว์เซอร์จริง ตรวจว่าโหลดโมดูลได้และ Console ไม่มีข้อความสีแดง

**งานถัดไป:** เจ้าของงานรัน 010 แล้วทดสอบหน้า `/history` ให้ผ่านก่อน จึงเริ่ม Phase 8 (หน้าพิมพ์)

### 2026-08-20 — Phase 7 ปิดงาน (เจ้าของงานทดสอบจริงผ่านครบ)
เจ้าของงานรัน `010_protect_issued_certificates.sql` และทดสอบหน้า `/history` จริงบนเบราว์เซอร์ผ่านครบทุกข้อ:
- ตัวกรองทั้ง 4 ตัวใช้ร่วมกันได้ และแถบสรุปเปลี่ยนตามตัวกรองจริง
- ค้นหาด้วยเลขที่เอกสาร ชื่อผู้ถูกหัก และเลขผู้เสียภาษี ได้ทั้ง 3 แบบ
- คัดลอกเป็นใบใหม่ได้ข้อมูลครบ วันที่เป็นวันนี้ และได้เลขที่ใหม่ ไม่ทับใบเดิม
- แก้ไขใบร่างแล้วบันทึกทับแถวเดิม จำนวนใบไม่เพิ่ม และใบที่ออกแล้วไม่มีปุ่มแก้ไขให้กด
- ยกเลิกเอกสารต้องกรอกเหตุผลก่อนเสมอ และแสดงเหตุผลในตารางหลังยกเลิก
- บัญชีผู้ใช้ทั่วไปมองไม่เห็นปุ่มยกเลิกเอกสาร
- ส่งออก CSV เปิดด้วย Excel แล้วภาษาไทยไม่เพี้ยน และเลขผู้เสียภาษี 13 หลักครบ ไม่กลายเป็นตัวเลขวิทยาศาสตร์
- ย่อหน้าจอเท่ามือถือแล้วตารางเปลี่ยนเป็นการ์ด ไม่ล้นจอ

**ช่องโหว่ที่เจอและปิดไปในเฟสนี้ (สรุปไว้เพื่อการตรวจย้อนหลัง)**
policy เดิมของ `wht_certificates` ในไฟล์ 004 เขียนไว้เพียงว่าต้องเป็นคนในองค์กรเดียวกัน
ทำให้ใครก็ได้ในองค์กรแก้เอกสารใบไหนก็ได้ รวมถึงใบที่ออกไปแล้ว ขัดกับ Spec ข้อ 4, 6.1 และข้อ 0
ปิดด้วยการป้องกัน 2 ชั้นในไฟล์ 010 คือ **ชั้น RLS** (แยก policy แก้ใบร่าง กับ ยกเลิกเอกสารเฉพาะ admin)
และ **ชั้น trigger** (คุมว่าเปลี่ยนค่าอะไรได้บ้าง) เพราะ RLS บอกได้แค่ว่าแตะแถวไหนได้ แต่บอกไม่ได้ว่าห้ามแก้คอลัมน์ไหน
ถ้ามีแต่ RLS ผู้ดูแลระบบจะยังแอบแก้ยอดเงินไปพร้อมกับการกดยกเลิกได้ในคำสั่งเดียว
พร้อมล็อกตาราง `wht_certificate_items` ด้วย กันกรณียอดรวมไม่ตรงกับรายละเอียดข้างใน

**การตัดสินใจที่เจ้าของงานอนุมัติแล้ว**
- หลังแก้ไขใบร่างเสร็จ ให้เด้งกลับหน้าประวัติเอกสารตามที่ออกแบบไว้ — ใช้ต่อได้ ไม่ต้องแก้
- ยังไม่เพิ่มปุ่มลบใบร่างในรอบนี้ รอไว้ก่อน ถ้าจำเป็นจะแจ้งเพิ่มภายหลัง

### 2026-08-21 — Phase 8: หน้าพิมพ์เอกสาร 50 ทวิ

**ที่มาของเค้าโครง**
เจ้าของงานเปิดแบบฟอร์มต้นฉบับของกรมสรรพากรดูเป็นภาพ แล้วบรรยายเค้าโครงมาให้ครบทุกส่วน
จึงใช้คำบรรยายนั้นเป็นความจริงหลักในการจัดวาง (ไม่ได้แกะจากไฟล์ PDF เอง)

**สิ่งที่ทำ**
- `src/lib/form-sections.js` — ข้อความหัวข้อประเภทเงินได้ 6 ข้อหลักพร้อมหัวข้อย่อยของข้อ 4 (ข) ครบทุกระดับ, ข้อความคำเตือนทางกฎหมาย, ข้อความรับรอง และหมายเหตุท้ายกระดาษ คัดตามต้นฉบับ
- `src/components/certificate-sheet.js` และ `certificate-footer.js` — วาดกระดาษ 1 แผ่น
- `src/pages/doc-view.js` + `src/styles/print.css` + `doc-view.css` — หน้า `/doc/:id`
- `src/lib/certificate-view.js` — โหลดข้อมูลสำหรับพิมพ์ และบันทึกประวัติการพิมพ์
- `src/lib/router.js` — เพิ่มความสามารถรองรับเส้นทางที่มีพารามิเตอร์ เช่น `/doc/:id`
- เปิดใช้ปุ่ม "ดู" และ "พิมพ์ซ้ำ" ในหน้าประวัติ และเพิ่มปุ่ม "บันทึกแล้วพิมพ์" ในหน้าสร้างเอกสาร

**บันทึกการตัดสินใจ**
- **จัดหน้าด้วยหน่วยมิลลิเมตรทั้งหมด** เพื่อให้ขนาดบนจอเท่ากับขนาดบนกระดาษจริง ไม่ต้องเดาสัดส่วน
- **เลขประจำตัวผู้เสียภาษีทำเป็นช่องสี่เหลี่ยม 13 ช่องแยกทีละหลัก** ตามแบบฟอร์มจริง โดยให้เส้นขอบซ้อนกันพอดีไม่หนาเป็นสองเท่า
- **บันทึกประวัติการพิมพ์ดักที่สัญญาณ beforeprint ของเบราว์เซอร์ ไม่ใช่ที่ปุ่ม** เพราะผู้ใช้กด Ctrl+P เองก็ถือเป็นการพิมพ์เหมือนกัน ดักที่เดียวจึงครอบคลุมทั้งสองทาง พร้อมตัวกันบันทึกซ้ำภายใน 3 วินาที
- **ใช้ที่อยู่ไฟล์โลโก้และลายเซ็นจาก payer_snapshot เป็นหลัก** ไม่ใช่จากตารางองค์กรปัจจุบัน เพราะถ้าบริษัทเปลี่ยนโลโก้ทีหลัง เอกสารเก่าที่พิมพ์ซ้ำต้องยังได้โลโก้เดิมตรงกับกระดาษที่ส่งให้ผู้รับเงินไปแล้ว
- **ตราประทับยกเลิกตั้งความทึบไว้เพียง 0.22** โดยตั้งใจ เพื่อให้ยังอ่านตัวเลขที่อยู่ข้างใต้ได้ตามที่กำหนดในเค้าโครง
- **ยอดเป็นตัวอักษรใช้ค่าที่บันทึกไว้ตอนออกเอกสารก่อน** ถ้าไม่มีจึงค่อยแปลงใหม่ เพื่อให้ตรงกับกระดาษที่เคยพิมพ์ออกไปแล้ว
- **ปิดคำถาม ❓ ใน Spec.md ข้อ 3.3** ยืนยันใช้เลขอารบิกทั้งเอกสาร

**วิธีตรวจสอบที่ใช้ก่อนส่งงาน**
1. `npm run test` ผ่าน 61 เคส และ `npm run build` ผ่าน
2. สคริปต์ตรวจ 3 อย่างที่ build มองไม่เห็น ผ่านทั้งหมด (วงจร import / callback ตอนสร้าง / ตัวแปรใช้ก่อนประกาศ)
3. เปิด `/doc/<รหัส>` ด้วยเบราว์เซอร์จริง ยืนยันว่าเส้นทางแบบมีพารามิเตอร์ทำงาน (เด้งไปหน้าล็อกอิน ไม่ใช่หน้าไม่พบ) และ Console ไม่มีข้อความสีแดง
4. **สร้างหน้าตัวอย่างชั่วคราวเพื่อดูเลย์เอาต์กระดาษด้วยข้อมูลสมมติ** แล้ววัดความสูงจริงได้ **277.0 มม. พอดีกับพื้นที่พิมพ์ของ A4** ยืนยันว่าลงได้ 1 หน้าโดยไม่ล้น พร้อมตรวจด้วยตาว่าช่อง 13 หลัก เช็คบ็อกซ์ ตาราง 6 ข้อ ตราประทับยกเลิก และส่วนลงชื่อ แสดงครบถูกตำแหน่ง จากนั้นลบไฟล์ชั่วคราวออกจากโปรเจกต์แล้ว

**งานถัดไป:** เจ้าของงานทดสอบพิมพ์จริงเทียบกับแบบฟอร์มต้นฉบับ ก่อนเริ่ม Phase 9

### 2026-08-21 — Phase 8 ปิดงาน (เจ้าของงานทดสอบจริงผ่านครบ)
เจ้าของงานทดสอบหน้าพิมพ์จริงผ่านครบทุกข้อ รวมถึงพิมพ์ลงกระดาษ A4 จริง:
- **พิมพ์ลงกระดาษจริงแล้ววางเทียบกับแบบฟอร์มต้นฉบับของกรมสรรพากร โครงตรงกัน**
- เลขประจำตัวผู้เสียภาษี 13 หลัก ลงครบทุกช่องทั้งฝั่งผู้จ่ายและผู้ถูกหัก
- คำอ่านตัวอักษรของยอดภาษีแสดงถูกต้อง
- **ตัวอักษรขนาด 6.1pt ของหัวข้อย่อยข้อ 4 (ข) ยังอ่านออกทั้งบนจอและบนกระดาษจริง จึงไม่ต้องย่อหรือพับเก็บ** (ปิดข้อกังวลที่ตั้งไว้ตอนส่งงาน)
- ตราประทับยกเลิกแสดงถูกต้องและยังอ่านตัวเลขที่อยู่ข้างใต้ออก
- ปุ่ม ดู / พิมพ์ซ้ำ / บันทึกแล้วพิมพ์ ใช้งานได้ทั้งหมด
- ตาราง `audit_logs` บันทึก `action = print` ถูกต้องทุกครั้งที่พิมพ์

**สรุปสถานะโครงการ:** ฟีเจอร์หลักของระบบครบแล้วทั้งหมด (ล็อกอิน ตั้งค่าองค์กร จัดการผู้ใช้ ทะเบียนผู้ถูกหักภาษี
ตรรกะคำนวณ สร้างเอกสาร ประวัติเอกสาร และพิมพ์เอกสาร) เหลือ Phase 9 (หน้าแรกและงานเก็บตก)
กับ Phase 10 (ตรวจความปลอดภัยและขึ้นระบบจริง)

### 2026-08-21 — Phase 9: หน้าแรก + ประวัติการใช้งาน + ตรวจทานทั้งระบบ

**คำตอบที่เจ้าของงานยืนยันแล้ว**
- หน้าแรกสรุปยอด **รวมทั้งบริษัท** ไม่แยกตามผู้สร้าง (ไม่ต้องแก้กฎ RLS ใด ๆ)
- หน้าดูประวัติการใช้งาน **เฉพาะ admin เท่านั้น**
- Deploy ใช้ **Netlify** ตามที่ CLAUDE.md ระบุไว้เดิม ไม่มีการเปลี่ยนแปลง

**ช่องโหว่ที่ตรวจพบและปิดแล้ว**
กฎ RLS เดิมของตาราง `audit_logs` ในไฟล์ 004 เปิดให้ **ทุกคนในองค์กรอ่านได้**
ขัดกับข้อกำหนดใหม่ที่ให้เฉพาะ admin ดูประวัติการใช้งาน
ถ้าซ่อนแค่เมนูบนหน้าจอ ผู้ใช้ทั่วไปที่รู้วิธีเรียกข้อมูลตรง ๆ ก็ยังดูได้อยู่ดี ซึ่งไม่ถือว่ากันสิทธิ์จริง
แก้ด้วย `011_audit_log_admin_only.sql` จำกัดสิทธิ์อ่านไว้ที่ admin
ส่วนสิทธิ์เขียนเพิ่มยังเป็นของทุกคนเหมือนเดิม เพราะผู้ใช้ทั่วไปต้องบันทึกประวัติได้ตอนสร้างและพิมพ์เอกสาร

**สิ่งที่ทำ**
- `src/lib/dashboard.js` + `src/pages/home.js` + `src/styles/home.css` — หน้าแรกสรุปยอดเดือนปัจจุบัน
- `src/lib/audit.js` + `src/pages/audit-log.js` + `src/styles/audit-log.css` — หน้าประวัติการใช้งาน
- เพิ่มเส้นทาง `/audit-log` และเมนูในแถบหัวเว็บที่แสดงเฉพาะ admin
- หน้าประวัติเอกสารรับตัวกรองสถานะจากที่อยู่เว็บได้ (`/history?status=draft`) เพื่อรองรับลิงก์จากหน้าแรก
- เติมกฎ CSS สำหรับจอมือถือให้หน้าสร้างเอกสารและหน้าพิมพ์เอกสาร ซึ่งเป็น 2 หน้าที่ยังไม่มี
- **ปิดเครื่องหมาย ❓ ที่เหลือใน Spec.md ครบทั้ง 4 จุด** และปรับเวอร์ชันสเปกเป็น 1.1

**บันทึกการตัดสินใจ**
- **ยังไม่มีเอกสารในเดือนนี้ ให้แสดงข้อความชวนสร้างใบแรก ไม่ใช่เลข 0 สามช่อง** เพราะเลข 0 เรียงกันไม่ได้บอกอะไร และผู้ใช้ใหม่จะไม่รู้ว่าต้องเริ่มตรงไหน
- **นับใบร่างค้างโดยไม่จำกัดเดือน** เพราะใบร่างที่ค้างมาจากเดือนก่อนก็ยังเป็นงานที่ต้องสะสางเหมือนกัน
- **หน้าประวัติการใช้งานต้องดึงข้อมูล 3 รอบ** เพราะตาราง `audit_logs` เก็บแค่รหัสผู้ใช้และรหัสเอกสาร ส่วนรหัสผู้ใช้ชี้ไปที่ระบบล็อกอินของ Supabase ซึ่งเชื่อมตรงกับตาราง `profiles` ไม่ได้ จึงต้องดึงชื่อผู้ใช้และเลขที่เอกสารมาจับคู่เองที่ฝั่งเว็บ โดยดึงเฉพาะเท่าที่หน้านั้นแสดงจริง
- **หน้าพิมพ์บนมือถือให้เลื่อนกระดาษดูทางแนวนอน ไม่ย่อขนาด** เพราะกระดาษกว้างคงที่เท่า A4 จริง ถ้าย่อ สิ่งที่เห็นบนจอจะไม่ตรงกับสิ่งที่ออกมาบนกระดาษ พร้อมข้อความบอกผู้ใช้ให้เลื่อนดู

**ผลการไล่ตรวจ error / empty state (งานที่ 3)**
ตรวจครบทั้ง 8 หน้า ทุกหน้ามีสถานะกำลังโหลดและข้อความผิดพลาดภาษาไทยครบ
ส่วนสถานะไม่มีข้อมูลมีอยู่ในทุกหน้าที่มีรายการให้แสดง (หน้าแรก ทะเบียน ประวัติเอกสาร ประวัติการใช้งาน)
หน้าที่ไม่มีสถานะไม่มีข้อมูลคือหน้าที่ไม่มีรายการ ได้แก่ เข้าสู่ระบบ ตั้งค่า สร้างเอกสาร และดูเอกสาร ซึ่งถูกต้องแล้ว
ทุกฟังก์ชันที่คุยกับฐานข้อมูลมีข้อความกรณีเน็ตหลุดเป็นภาษาไทยครบ

**วิธีตรวจสอบที่ใช้ก่อนส่งงาน**
1. `npm run test` ผ่าน 61 เคส และ `npm run build` ผ่าน
2. เปิด `/audit-log` ด้วยเบราว์เซอร์จริง เส้นทางทำงานถูกต้อง (เด้งไปหน้าล็อกอิน ไม่ใช่หน้าไม่พบ) Console ไม่มีข้อความสีแดง
3. ไล่ตรวจกฎ CSS สำหรับจอแคบของทุกไฟล์สไตล์ พบว่าขาด 2 ไฟล์จึงเติมให้ครบ

**งานถัดไป:** เจ้าของงานรัน 011 แล้วทดสอบ Phase 9 ก่อนเริ่ม Phase 10 ซึ่งเป็นเฟสสุดท้าย

---

## ขั้นตอน deploy จริงที่เจ้าของงานต้องทำเอง (Phase 10)

> ทำหลังสมัครบัญชี Netlify ด้วยอีเมลลูกค้าเรียบร้อยแล้ว
> ไฟล์ตั้งค่าทั้งหมดเตรียมไว้ให้แล้ว จึงแทบไม่ต้องกรอกอะไรเองในหน้าเว็บ Netlify

### ขั้นที่ 0 — สร้าง git repo ก่อน (สำคัญ ยังไม่ได้ทำ)
โปรเจกต์นี้ **ยังไม่เคยถูกทำเป็น git repo เลย** (ตรวจแล้วไม่มีโฟลเดอร์ `.git`)
Netlify ต้องเชื่อมกับ GitHub จึงจะ deploy อัตโนมัติได้ ต้องทำขั้นนี้ก่อนเสมอ

1. สร้าง repository ใหม่บน GitHub แบบ **Private** (ห้ามเป็น Public เพราะเป็นระบบข้อมูลภาษี)
2. ในโฟลเดอร์โปรเจกต์ รันคำสั่ง `git init` แล้ว `git add .` และ `git commit`
3. **ก่อน commit ครั้งแรก ตรวจให้แน่ใจว่าไฟล์ `.env` ไม่ติดไปด้วย** โดยรัน `git status` แล้วดูว่าไม่มีชื่อ `.env` อยู่ในรายการ
   (ไฟล์ `.gitignore` กันไว้ให้แล้ว แต่ควรตรวจด้วยตาอีกครั้งเพราะแก้ทีหลังยาก)
4. push ขึ้น GitHub

### ขั้นที่ 1 — เชื่อม Netlify กับ GitHub
1. เข้า Netlify กด **Add new site** แล้วเลือก **Import an existing project**
2. เลือก **GitHub** แล้วอนุญาตให้ Netlify เข้าถึง repository ที่เพิ่งสร้าง
3. เลือก repository ของโปรเจกต์นี้

### ขั้นที่ 2 — ตรวจค่า build
Netlify จะอ่านไฟล์ `netlify.toml` ในโปรเจกต์เองอัตโนมัติ จึงควรขึ้นค่าเหล่านี้ให้แล้ว
- **Build command:** `npm run build`
- **Publish directory:** `dist`

ถ้าไม่ขึ้นตามนี้ให้พิมพ์แก้ให้ตรง แล้วกด **Deploy**

### ขั้นที่ 3 — ใส่ค่าเชื่อมต่อฐานข้อมูล
ทำตามไฟล์ `DEPLOY_ENV.md` ที่เตรียมไว้ ต้องใส่ 2 ค่าคือ
`VITE_SUPABASE_URL` และ `VITE_SUPABASE_ANON_KEY`

**ใส่ค่าแล้วต้องสั่ง deploy ใหม่อีกครั้ง** (Deploys → Trigger deploy → Clear cache and deploy site)
เพราะค่าเหล่านี้ถูกฝังเข้าไฟล์เว็บตอน build ถ้าไม่ deploy ใหม่ เว็บจะยังขึ้นว่าเชื่อมต่อฐานข้อมูลไม่ได้

### ขั้นที่ 4 — ทดสอบที่อยู่เว็บจริง
1. เปิดที่อยู่ที่ Netlify ให้มา (แบบ `ชื่อ.netlify.app`) ต้องเห็นหน้าเข้าสู่ระบบ
2. ล็อกอินแล้วลองใช้งานให้ครบทุกหน้า
3. **เข้าหน้า `/history` แล้วกด F5 รีเฟรช ต้องไม่ขึ้น 404** ข้อนี้พิสูจน์ว่ากฎ redirect ทำงาน
4. ลองสร้างและพิมพ์เอกสาร 1 ใบบนเว็บจริง

### ขั้นที่ 5 — เก็บกวาดก่อนส่งมอบ
ลบบัญชีทดสอบตามรายการในหัวข้อถัดไป และเปลี่ยนบัญชีผู้ดูแลระบบเป็นอีเมลลูกค้า

---

## รายการบัญชีที่ต้องจัดการก่อนส่งมอบ (Phase 10 งานที่ 3)

> **ยังไม่ได้ลบอะไรทั้งสิ้น** เป็นเพียงรายการให้เจ้าของงานตัดสินใจ
> ดูรายชื่อจริงได้ที่ Supabase → Authentication → Users

| บัญชี | สถานะปัจจุบัน | ควรทำอย่างไร |
|---|---|---|
| `1896c817-eb78-4549-bd55-17fe77cec197` (อีเมลผู้พัฒนา) | ผู้ดูแลระบบ ใช้ทดสอบมาตลอดทุกเฟส | **เก็บไว้ก่อนจนกว่าจะสร้างบัญชีผู้ดูแลของลูกค้าเสร็จและทดสอบว่าเข้าได้จริง แล้วจึงลบทิ้ง** ห้ามลบก่อน ไม่งั้นจะไม่เหลือผู้ดูแลระบบเลย |
| `test-user@example.com` | ผู้ใช้งานทั่วไป สร้างไว้ทดสอบสิทธิ์ตอน Phase 3.5 | **ลบทิ้ง** ไม่มีความจำเป็นต้องใช้ต่อ |
| บัญชีผู้ดูแลระบบของลูกค้า | **ยังไม่ได้สร้าง** | **ต้องสร้างใหม่** ด้วยอีเมลจริงของลูกค้า แล้วตั้งเป็น admin |
| บัญชีพนักงานที่จะใช้งานจริง | **ยังไม่ได้สร้าง** | สร้างตามจำนวนคนที่ลูกค้าต้องการ ทุกคนเริ่มเป็นผู้ใช้งานทั่วไปโดยอัตโนมัติ |

**ลำดับที่ปลอดภัยที่สุด**
1. สร้างบัญชีผู้ดูแลระบบของลูกค้า
2. ตั้งบัญชีนั้นเป็น admin ผ่านหน้าตั้งค่าในเว็บ
3. **ล็อกอินด้วยบัญชีใหม่แล้วยืนยันว่าเข้าได้และเห็นเมนูครบ**
4. จึงค่อยลบบัญชีผู้พัฒนาและบัญชีทดสอบ

> การลบบัญชีใน Supabase จะลบแถวใน `profiles` ตามไปด้วยอัตโนมัติ
> แต่**เอกสารที่บัญชีนั้นเคยสร้างจะไม่หาย** เพราะออกแบบให้ช่องผู้สร้างกลายเป็นค่าว่างแทนการลบเอกสารตาม

---

### 2026-08-21 — Phase 10 (ส่วนที่ทำได้โดยไม่ต้องพึ่งบัญชี Netlify)

**บริบทที่เจ้าของงานยืนยัน**
- บัญชี Supabase เป็นของลูกค้าอยู่แล้วตั้งแต่แรก ไม่ต้องโอนกรรมสิทธิ์
- Deploy ใช้ Netlify โดเมนฟรี เจ้าของงานจะสมัครด้วยอีเมลลูกค้าเองแล้วส่งมอบรหัสตอนจบงาน

**งานที่ 1 — ตรวจ RLS ข้ามองค์กร: เตรียมเสร็จ รอรัน**
สร้าง `supabase/migrations/999_verify_cross_org.sql` ซึ่งไม่ใช่ migration แต่เป็นชุดทดสอบ
วิธีที่เลือกใช้คือ **สร้างองค์กรปลอมขึ้นมาชั่วคราวในรายการเดียว แล้วยกเลิกทั้งหมดทิ้งเมื่อจบ (rollback)**
จึงทดสอบได้ครบทั้ง 6 ตารางโดย **ไม่มีข้อมูลทดสอบค้างในฐานข้อมูลจริงแม้แต่แถวเดียว**
เลือกวิธีนี้แทนการสมัครบัญชีจริงและสร้างบริษัทที่สอง เพราะวิธีนั้นจะเหลือข้อมูลค้างตอนส่งมอบ
ชุดทดสอบครอบคลุมทั้งการ **อ่าน** ข้ามองค์กร และการ **เขียน** ข้ามองค์กร

**งานที่ 2 — ตรวจคีย์ลับหลุด: เสร็จสมบูรณ์ ไม่พบปัญหา**
- ค้นคำว่า `service_role` ทั้งโปรเจกต์ พบ 4 จุด **ตรวจแล้วปลอดภัยทุกจุด** เป็นข้อความในกฎ 1 จุด คอมเมนต์เตือน 1 จุด และคำสั่งให้สิทธิ์แก่ **ชื่อบทบาท** (ไม่ใช่ตัวกุญแจ) ในไฟล์ 004 อีก 2 จุด
- ค้นหารูปแบบของกุญแจ (ขึ้นต้นด้วย `eyJ`) และที่อยู่ Supabase ฝังในโค้ด — **ไม่พบเลย**
- `.env.example` มีอยู่จริงและ **ไม่มีค่าจริงปนอยู่** มีแต่ชื่อตัวแปรเปล่า
- `.gitignore` กัน `.env` ไว้เรียบร้อย
- **ข้อค้นพบสำคัญ: โปรเจกต์นี้ยังไม่เคยถูกทำเป็น git repo เลย** จึงไม่มีประวัติ commit ให้ตรวจ และแปลว่า `.env` ไม่มีทางเคยหลุดขึ้น git ได้ แต่ก็แปลว่า **ต้องสร้าง repo ก่อนถึงจะ deploy ผ่าน Netlify ได้** (เพิ่มเป็นขั้นที่ 0 ในขั้นตอน deploy แล้ว)

**งานที่ 3 — รายการบัญชีที่ต้องจัดการ: เตรียมรายการเสร็จ ยังไม่ได้ลบอะไร**
ดูตารางในหัวข้อ "รายการบัญชีที่ต้องจัดการก่อนส่งมอบ" ด้านบน

**งานที่ 4 — ไฟล์ตั้งค่า deploy: เสร็จสมบูรณ์**
- `netlify.toml` — คำสั่ง build, โฟลเดอร์ผลลัพธ์ (**ตรวจแล้วว่า `npm run build` สร้างโฟลเดอร์ `dist` จริง เพราะโปรเจกต์ไม่มีไฟล์ vite.config จึงใช้ค่าตั้งต้นของ Vite**), กฎส่งทุกที่อยู่กลับไป index.html และหัวข้อมูลด้านความปลอดภัยพื้นฐาน
- `DEPLOY_ENV.md` — รายการตัวแปรที่ต้องกรอกใน Netlify **โดยไม่มีค่าจริงอยู่ในไฟล์**

**งานที่ 5 — คู่มือใช้งาน: เสร็จสมบูรณ์ รอเติมช่องทางติดต่อ**
`USER_GUIDE.md` 10 หัวข้อ ภาษาไทยล้วน ไม่ใช้ศัพท์เทคนิค
มีตารางอาการที่พบบ่อยพร้อมวิธีแก้ด้วยตัวเอง 8 รายการ
**เว้นช่องว่างไว้ให้เจ้าของงานเติมช่องทางติดต่อเอง 4 ช่อง**
ไม่มีภาพหน้าจอประกอบ เพราะไม่สามารถล็อกอินเข้าระบบเพื่อถ่ายภาพได้ จึงใช้คำอธิบายละเอียดแทน

**งานที่ 6 — ขั้นตอน deploy ที่เหลือ: เขียนเป็นเอกสารเสร็จแล้ว**
ดูหัวข้อ "ขั้นตอน deploy จริงที่เจ้าของงานต้องทำเอง" ด้านบน แบ่งเป็นขั้นที่ 0 ถึงขั้นที่ 5

**สิ่งที่ยังเหลือใน Phase 10** (ปรับปรุง 21 ส.ค. 2026 — ตรวจ RLS ผ่านแล้ว จึงตัดออกจากรายการ)
- สร้าง git repo และ push ขึ้น GitHub
- สมัคร Netlify แล้ว deploy ตามขั้นตอน
- เก็บกวาดบัญชีทดสอบและเปลี่ยนผู้ดูแลระบบเป็นอีเมลลูกค้า
- เติมช่องทางติดต่อใน `USER_GUIDE.md`
- ตอบคำถามค้าง A4, A5, E20-E24 ที่ยังไม่ได้คุย

---

### 2026-08-21 — แก้ชุดทดสอบ RLS ข้ามองค์กร (รันไม่ผ่านรอบแรก)

**อาการ:** รัน `999_verify_cross_org.sql` แล้วขึ้น error
"เอกสารนี้ออกไปแล้ว แก้ไขรายการเงินได้ไม่ได้" จากฟังก์ชัน `protect_issued_certificate_items()`

**สาเหตุ: สคริปต์ทดสอบเองเรียงลำดับผิด ไม่ใช่บั๊กของระบบ**
สคริปต์รอบแรกสร้างเอกสารปลอมเป็นสถานะ `issued` ตั้งแต่คำสั่งแรก แล้วค่อยเพิ่มรายการเงินได้ทีหลัง
จึงไปชนกฎกันแก้ไขเอกสารที่ออกแล้วซึ่งเพิ่มไว้ใน migration 010
**กฎนั้นทำงานถูกต้องแล้ว** การที่ error ขึ้นคือหลักฐานว่ากฎกันได้จริงตามที่ออกแบบไว้

**วิธีแก้ที่เลือกใช้**
เปลี่ยนลำดับการเตรียมข้อมูลทดสอบให้เป็นลำดับเดียวกับที่ระบบจริงใช้ตอนออกเอกสาร คือ
สร้างเป็นใบร่างก่อน แล้วใส่รายการเงินได้ แล้วจึงเปลี่ยนสถานะเป็นออกแล้วเป็นขั้นสุดท้าย

**เลือกวิธีนี้แทนการปิดกฎชั่วคราวด้วย `session_replication_role = replica`** ด้วย 2 เหตุผล
1. ไม่ต้องปิดกลไกความปลอดภัยของฐานข้อมูลจริงแม้ชั่วคราว ถ้าสคริปต์หยุดกลางคันก่อนเปิดกลับ จะเหลือฐานข้อมูลที่ trigger ไม่ทำงาน
2. ได้ผลพลอยได้คือพิสูจน์ไปในตัวว่าลำดับที่ระบบจริงใช้ตอนออกเอกสารนั้นถูกต้อง

**ปรับปรุงเพิ่มระหว่างแก้**
- เปลี่ยนการอ้างอิงข้อมูลทดสอบจากการเทียบค่าที่พิมพ์เอง มาเป็นอ้างด้วยรหัสจริงที่จำไว้ตั้งแต่ตอนสร้าง กันไม่ให้เผลอนับข้อมูลจริงของลูกค้าว่าเป็นข้อมูลทดสอบ
- เพิ่มการทดสอบข้อ 5.2 ลองแก้ข้อมูลขององค์กรอื่น เพิ่มจากเดิมที่ทดสอบแค่การเพิ่มข้อมูล
- **แยกขั้นที่ 7 ออกไปรันต่างหาก** เพราะคำสั่งที่ต่อท้าย `rollback` ในการรันครั้งเดียวกันจะทำงานไม่ได้

**ยังไม่ได้รันทดสอบเอง** เครื่องนี้ไม่มี PostgreSQL และ Docker ไม่ได้เปิดอยู่
ตรวจได้เพียงโครงสร้างไฟล์และไล่ตรรกะเทียบกับเงื่อนไขใน trigger ทีละขั้น

### 2026-08-21 — ตรวจสอบผลขั้นที่ 4 ที่ดูเหมือนเจอช่องโหว่ (สรุป: ไม่ใช่ช่องโหว่)

**สิ่งที่เจ้าของงานเห็น:** ขั้นที่ 4 ของชุดทดสอบ RLS ควรได้ 0 rows แต่กลับได้ตารางที่มีตัวเลข
organizations 2, payees 3, wht_certificates 2, doc_counters 1, audit_logs 5

**ข้อสรุป: ไม่ใช่ช่องโหว่ กฎ RLS ไม่ได้รั่ว** พิสูจน์จากตัวเลขที่เห็นเองได้ดังนี้

เงื่อนไขที่ใช้คือ `where id is distinct from public.get_my_org_id()`
ถ้าฟังก์ชันคืนรหัสองค์กรจริง ตาราง organizations จะนับได้มากที่สุด 1 แถว (คือองค์กรปลอม)
เพราะองค์กรของตัวเองไม่มีทางเข้าเงื่อนไข ไม่ใช่องค์กรตัวเอง ได้
**การที่นับได้ 2 จึงแปลว่าฟังก์ชันคืนค่าว่าง** ทำให้เงื่อนไขเป็นจริงกับทุกแถวรวมองค์กรของตัวเองด้วย

และถ้าฟังก์ชันคืนค่าว่างขณะที่กฎ RLS ทำงานอยู่จริง ทุก policy จะเทียบไม่ติดและมองไม่เห็นอะไรเลย ผลต้องเป็น 0
**การที่ยังนับได้ จึงแปลว่าคำสั่งนั้นทำงานด้วยสิทธิ์เจ้าของฐานข้อมูล ซึ่งข้ามกฎ RLS ได้ตามปกติของ PostgreSQL**
สาเหตุคือคำสั่งขั้นที่ 4 ถูกรันนอกบริบทการสวมบทเป็นผู้ใช้ ไม่ได้รันต่อเนื่องมาจากขั้นที่ 2

**ต้นเหตุจริงคือชุดทดสอบที่ผมเขียนเปราะเกินไป** ไม่ได้ตรวจก่อนว่าการสวมบทได้ผลจริงหรือยัง
จึงรายงานผลออกมาน่าตกใจทั้งที่การทดสอบยังไม่ได้เริ่มทำงานด้วยซ้ำ

**สิ่งที่แก้**
- เพิ่ม **ขั้นที่ 2.5 ด่านตรวจ** หยุดทั้งสคริปต์ทันทีพร้อมบอกสาเหตุ ถ้าสิทธิ์ไม่ใช่ `authenticated` หรือหาสังกัดองค์กรไม่เจอ
- เพิ่มคอลัมน์ **สิทธิ์ที่ใช้อยู่** และ **องค์กรของฉัน** ในผลลัพธ์ขั้นที่ 4 เพื่อให้อ่านบริบทได้ทันทีโดยไม่ต้องเดา
- เปลี่ยนชื่อคอลัมน์จาก แถวที่ข้ามองค์กร เป็น **แถวที่ไม่ใช่ขององค์กรตัวเอง** เพราะชื่อเดิมสื่อว่าเจอการรั่วไหลแล้ว ทั้งที่ยังสรุปแบบนั้นไม่ได้
- เพิ่มคำเตือนชัดเจนว่าขั้นที่ 4 **ห้ามคัดลอกไปรันเดี่ยว ๆ** ต้องรันพร้อมทั้งไฟล์เท่านั้น
- เพิ่ม **คำสั่งวินิจฉัยด่วน** ไว้ต้นไฟล์ ใช้ดูว่าตอนนี้ทำงานด้วยสิทธิ์อะไรและเห็นข้อมูลกี่แถว

**บทเรียน:** ชุดทดสอบความปลอดภัยต้องตรวจก่อนเสมอว่าตัวมันเองเริ่มทำงานถูกเงื่อนไขหรือยัง
ไม่งั้นผลลบลวงจะน่ากลัวพอ ๆ กับช่องโหว่จริง และเสียเวลาไล่หาสาเหตุโดยไม่จำเป็น

### 2026-08-21 — ตรวจความปลอดภัย RLS ข้ามองค์กร ผ่านครบทุกขั้น

เจ้าของงานรัน `999_verify_cross_org.sql` ใหม่ทั้งไฟล์ในครั้งเดียวตามวิธีที่แก้ไว้ **ผลผ่านครบทุกขั้น**
- ด่านตรวจขั้นที่ 2.5 ผ่าน ยืนยันว่าการสวมบทเป็นผู้ใช้ได้ผลจริง จึงอ่านผลขั้นถัด ๆ ไปได้อย่างเชื่อถือได้
- ขั้นที่ 3 มองไม่เห็นข้อมูลขององค์กรอื่นเลยทั้ง 6 ตาราง
- ขั้นที่ 4 ไม่มีแถวใดที่ไม่ใช่ขององค์กรตัวเอง
- ขั้นที่ 5 เขียนและแก้ข้อมูลข้ามองค์กรถูกปฏิเสธทั้ง 2 ข้อ
- ขั้นที่ 7 ไม่มีข้อมูลทดสอบหลงเหลือในฐานข้อมูลจริง

**สรุป: กฎ RLS กันข้อมูลข้ามองค์กรได้จริงทั้งการอ่านและการเขียน** ปิดข้อตรวจความปลอดภัยข้อสุดท้ายของ Phase 10

---

### 2026-08-21 — ปรับ layout ทั้งระบบ: ขยายความกว้างหน้าจอ + ปุ่มกลุ่มไม่ทับกัน

งานนี้เจ้าของงานสั่งเพิ่มหลังปิด Phase 10 เป็นการปรับหน้าตา ไม่กระทบตรรกะภาษีหรือฐานข้อมูล

**เรื่องที่ 1 — เนื้อหาถูกบีบไว้กลางจอ ไม่เต็มพื้นที่**

ปัญหา: ทุกหน้าจำกัดความกว้างไว้ที่ 960px (หน้าดูเอกสาร 1040px) ลูกค้าใช้งานผ่านคอมเป็นหลัก
จึงเหลือพื้นที่ว่างซ้าย-ขวามากเกินไป

วิธีแก้: สร้างค่ากลาง 2 ตัวใน `tokens.css` แล้วให้ทุกจุดอ้างค่าเดียวกัน
- `--page-max-width: 1600px` (เดิม 960px — ปรับเป็น 1400px ก่อน แล้วเจ้าของงานขอเพิ่มเป็น 1600px)
- `--page-padding-inline: clamp(16px, 2.5vw, 32px)` ระยะขอบซ้าย-ขวายืดตามขนาดจอ

ทำไมเลือกวิธีนี้: เดิมเลข 960px กระจายอยู่ 3 ไฟล์ ถ้าแก้ทีละไฟล์จะหลุดง่ายและขอบของแถบเมนู
กับตัวเนื้อหาจะไม่ตรงกัน การรวมเป็นค่ากลางทำให้แก้จุดเดียวเปลี่ยนทั้งเว็บ และรับประกันว่า
แถบหัวเว็บกับเนื้อหาจะเรียงขอบตรงกันเสมอ

ทำไมไม่ปล่อยเต็มจอ 100%: บนจอกว้างมาก บรรทัดข้อความจะยาวจนกวาดตาอ่านลำบาก และตารางจะยืด
จนช่องว่างระหว่างคอลัมน์ห่างเกินไป

จุดที่ตั้งใจไม่ขยาย: หน้าเข้าสู่ระบบ (กล่องล็อกอินควรอยู่กลางจอ) และตัวกระดาษ A4 ในหน้าดูเอกสาร
(ล็อกไว้ 190 มม. เท่ากระดาษจริง ขยายไม่ได้) — กระดาษยังอยู่กึ่งกลางเหมือนเดิม เหมือนโปรแกรมเปิด PDF

**เรื่องที่ 2 — ปุ่มที่อยู่เป็นกลุ่มเกยทับกันเวลาย่อความกว้างจอ**

ปัญหา: แถวปุ่มแต่ละหน้าเขียนกฎ flex ซ้ำกันเองคนละที่ บางที่ใส่ `flex-wrap` บางที่ลืม
ที่ลืมจะดันปุ่มล้นออกนอกกรอบเมื่อพื้นที่ไม่พอ

วิธีแก้: สร้างคลาสกลาง `.btn-group` ใน `app.css` ใช้ร่วมกันทุกหน้า (พร้อมตัวปรับ `--tight` สำหรับ
ปุ่มเล็กในตาราง และ `--end` สำหรับปุ่มยืนยันท้ายฟอร์ม) แล้วไล่เปลี่ยนแถวปุ่มทุกหน้ามาใช้คลาสนี้
รวม 13 จุด พร้อมลบกฎ flex ที่ซ้ำซ้อนออกจากไฟล์ CSS รายหน้า

เพิ่มเติมที่ `.btn`: ใส่ `white-space: nowrap` และ `flex-shrink: 0` เพื่อไม่ให้ตัวหนังสือหักบรรทัด
กลางปุ่มหรือถูกบีบจนแคบกว่าข้อความ เมื่อที่ไม่พอให้ตกไปทั้งปุ่มที่บรรทัดใหม่แทน

ทำไมเลือกวิธีนี้แทนการไล่ใส่ `flex-wrap` ทีละที่: การไล่แก้ทีละจุดจะเกิดปัญหาเดิมซ้ำทุกครั้งที่
เพิ่มปุ่มใหม่ในอนาคต การมีคลาสกลางทำให้จุดใหม่แค่ใส่ `btn-group` ก็ได้พฤติกรรมถูกต้องทันที
และเวลาลูกค้าขอปรับระยะห่างระหว่างปุ่มก็แก้ที่เดียวจบ

จุดที่แก้เพิ่มระหว่างทาง: ตารางจัดการผู้ใช้ในหน้าตั้งค่า เดิมป้ายสถานะกับปุ่มเปิด/ปิดวางชิดกัน
ด้วย `margin-left` ลอย ๆ และมีกฎ `display: table-cell` ที่ทำให้ช่องนี้ไม่ยุบเป็นการ์ดตามช่องอื่น
บนมือถือ เปลี่ยนมาห่อด้วย `.btn-group` แทน แล้วลบกฎเก่าทั้งสองออก

**วิธีทดสอบที่ใช้ (ไม่ต้องล็อกอิน)**

`npm run build` และ `npm test` (61 เคส) ผ่านทั้งคู่ แต่สองอย่างนี้จับปัญหาหน้าตาไม่ได้
จึงทดสอบเพิ่มด้วยวิธีนี้:
1. สร้างหน้าทดสอบแยกที่โหลด CSS ตัวจริงที่ build ออกมา แล้ววางแถวปุ่มของทั้ง 11 จุดจริง
   ลงในกรอบความกว้าง 7 ขนาด (1600 / 1200 / 900 / 700 / 520 / 400 / 320 px) รวม 77 กลุ่มปุ่ม
2. เปิดใน Chrome จริง แล้วรันสคริปต์วัดพิกัดจริงของทุกปุ่ม ตรวจ 3 อย่าง:
   ปุ่มทับกันเอง / ปุ่มล้นออกนอกกรอบ / เนื้อหาล้นแนวนอน — **ผลคือไม่พบปัญหาเลยทั้ง 77 กลุ่ม**
3. ทดสอบย้อนกลับเพื่อพิสูจน์ว่าตัวตรวจจับใช้ได้จริง ไม่ใช่ผ่านเพราะตรวจไม่เป็น:
   ปิด `flex-wrap` ชั่วคราวแล้ววัดซ้ำ ตัวตรวจจับรายงานปัญหา 21 จุดทันที (เช่นปุ่ม "บันทึกแล้วพิมพ์"
   ล้นออกนอกกรอบ 81px ที่ความกว้าง 520px) เมื่อเปิดกลับก็กลับเป็น 0 ตามเดิม

สิ่งที่ยังทดสอบไม่ได้เพราะไม่มีบัญชีเข้าระบบ: การแสดงผลของหน้าจริงที่มีข้อมูลจากฐานข้อมูล
เจ้าของงานแจ้งว่าทดสอบด้วยตัวเองแล้วผ่านครบ

**ข้อห้ามที่ระวังตลอดงานนี้:** ไม่แตะ `print.css` และไม่แตะบล็อก `@media print` ใด ๆ เลย
เพราะทดสอบกับกระดาษ A4 จริงผ่านแล้วใน Phase 8 — ตรวจยืนยันด้วย `git diff` ทุกครั้งก่อน commit

**ไฟล์ที่เปลี่ยน**

CSS
- `src/styles/tokens.css` — เพิ่มค่ากลาง `--page-max-width` และ `--page-padding-inline`
- `src/styles/app.css` — `.page` ใช้ค่ากลาง, เพิ่มคลาสกลาง `.btn-group` / `--tight` / `--end`, `.btn` กันข้อความหักบรรทัด
- `src/styles/navbar.css` — แถบเมนูใช้ค่ากลางเดียวกับเนื้อหา
- `src/styles/doc-view.css` — `.doc-page` ใช้ค่ากลาง, `.doc-toolbar__controls` เหลือแค่การจัดฐานให้เสมอกัน
- `src/styles/history.css` — ลบกฎ flex ซ้ำของแถวปุ่มในตารางและกล่องยกเลิกเอกสาร
- `src/styles/payees.css` — ลบกฎ flex ซ้ำของแถวปุ่มในตาราง แถบแบ่งหน้า และฟอร์มรายชื่อ
- `src/styles/settings.css` — ลบกฎ flex ซ้ำของปุ่มโลโก้/ลายเซ็น และลบ margin/`display: table-cell` ของตารางผู้ใช้
- `src/styles/new-certificate.css` — `.new-cert__actions` เหลือแค่การติดขอบล่างจอ, เพิ่ม `.income-lines__manual`
- `src/styles/home.css` — ลบกฎ flex ซ้ำของปุ่มลัด

หน้าจอ/ชิ้นส่วน (เปลี่ยนเฉพาะชื่อคลาสของกล่องที่ห่อปุ่ม ไม่แตะตรรกะ)
- `src/pages/home.js`, `src/pages/history.js`, `src/pages/payees.js`, `src/pages/audit-log.js`,
  `src/pages/new-certificate.js`, `src/pages/doc-view.js`
- `src/components/history-table.js`, `src/components/payee-table.js`, `src/components/payee-form.js`,
  `src/components/void-form.js`, `src/components/asset-upload.js`, `src/components/user-manager.js`,
  `src/components/income-lines.js`

**หมายเหตุเรื่อง git:** ระหว่างงานนี้โปรเจกต์ถูก push ขึ้น GitHub เรียบร้อยแล้วที่
`github.com/tongmovo/50lib` (สาขา `main`) — งานข้อ 1 ในตาราง "สิ่งที่เจ้าของงานต้องทำเอง"
ด้านล่างจึงทำไปแล้ว เหลือแต่การเชื่อม Netlify

---

## สถานะโครงการ ณ 21 ส.ค. 2026

**งานฝั่งพัฒนาเสร็จครบทั้งหมดแล้ว** Phase 0 ถึง Phase 9 ปิดสมบูรณ์ทุกเฟส
และ Phase 10 ส่วนที่ทำได้โดยไม่ต้องพึ่งบัญชีจริงก็เสร็จแล้วเช่นกัน

### สิ่งที่เหลือทั้งหมด แบ่งเป็น 2 กลุ่ม

**กลุ่มที่ 1 — งานที่เจ้าของงานต้องทำเอง (เรียงตามลำดับที่ควรทำ)**

| ลำดับ | งาน | ทำไมต้องเป็นเจ้าของงาน |
|---|---|---|
| 1 | **สร้าง git repo แบบ Private แล้ว push ขึ้น GitHub** | โปรเจกต์ยังไม่เคยเป็น git repo และ Netlify ต้องเชื่อมกับ GitHub จึงจะ deploy ได้ (ดูขั้นที่ 0 ในหัวข้อขั้นตอน deploy) |
| 2 | **สมัคร Netlify ด้วยอีเมลลูกค้า แล้ว deploy** | ต้องใช้บัญชีจริงของลูกค้า (ดูขั้นที่ 1-4 ในหัวข้อขั้นตอน deploy และไฟล์ `DEPLOY_ENV.md`) |
| 3 | **เติมช่องทางติดต่อใน `USER_GUIDE.md`** | มี 4 ช่องเว้นไว้ที่หัวข้อ 10 ท้ายไฟล์ ต้องใช้ข้อมูลจริงของผู้ดูแลและผู้พัฒนา |
| 4 | **สร้างบัญชีผู้ดูแลระบบของลูกค้า แล้วเก็บกวาดบัญชีทดสอบ** | ต้องทำหลัง deploy เสร็จ และต้องทดสอบว่าบัญชีใหม่เข้าได้จริงก่อนลบบัญชีเก่า (ดูตารางในหัวข้อรายการบัญชี) |
| 5 | **ส่งมอบและสอนใช้งาน** | ขั้นสุดท้าย |

**กลุ่มที่ 2 — คำถามค้างที่ยังไม่ได้คุยกับลูกค้า 6 ข้อ**

| ข้อ | คำถาม | ผลกระทบ |
|---|---|---|
| **A4** | มีไฟล์ Excel รายชื่อผู้ถูกหักภาษีเดิมอยู่แล้วหรือไม่ | ถ้ามี อาจต้องทำฟีเจอร์นำเข้า ซึ่งพักไว้ก่อนแล้ว ถือเป็นงานเพิ่ม |
| **A5** | ออกเอกสารประมาณกี่ใบต่อเดือน | ใช้ประเมินว่าแพ็กเกจฟรีของ Supabase เพียงพอหรือไม่ |
| **E20** | ต้องการไฟล์สำหรับยื่น ภ.ง.ด.3 / ภ.ง.ด.53 ตามรูปแบบกรมสรรพากรหรือไม่ | **งานใหญ่ ยังไม่เคยตกลงขอบเขต ควรคิดแยกเป็นงานเพิ่ม** ไฟล์ CSV ที่มีอยู่ตอนนี้เป็นรายการสรุปธรรมดา ไม่ใช่รูปแบบยื่นภาษี |
| **E21** | ต้องการส่งอีเมลแนบ PDF ให้ผู้ถูกหักภาษีอัตโนมัติหรือไม่ | ระบุไว้แต่แรกว่าเป็นเฟสถัดไป |
| **E22** | ใครเป็นคนจ่ายค่าบริการ Supabase และ hosting | กระทบการส่งมอบและค่าใช้จ่ายระยะยาว |
| **E23-E24** | ต้องการโดเมนของตัวเองหรือไม่ และกำหนดส่งมอบเมื่อไหร่ | ตกลงใช้โดเมนฟรีของ Netlify ไปก่อน ถ้าจะเปลี่ยนต้องตั้งค่าเพิ่ม |
