# Spec.md — ระบบออกหนังสือรับรองการหักภาษี ณ ที่จ่าย (50 ทวิ)

รหัสโปรเจกต์: `50bis`
เวอร์ชันสเปก: 1.1 (21 ส.ค. 2026 — ลูกค้ายืนยันครบทุกข้อแล้ว ไม่มีข้อค้างที่ทำเครื่องหมาย ❓ เหลืออยู่)
ฐานข้อมูล: Supabase (PostgreSQL)

---

## 0. ภาพรวมระบบ

เว็บแอปสำหรับ **ผู้จ่ายเงิน (ผู้มีหน้าที่หักภาษี ณ ที่จ่าย)** ใช้ออกเอกสาร "หนังสือรับรองการหักภาษี ณ ที่จ่าย ตามมาตรา 50 ทวิ แห่งประมวลรัษฎากร" ให้ผู้รับเงิน โดยเก็บข้อมูลทั้งหมดบนคลาวด์ เปิดใช้ผ่านเบราว์เซอร์ได้ทุกที่

**ผู้ใช้งาน 2 ระดับ**
| บทบาท | ทำอะไรได้ |
|---|---|
| `admin` | ทุกอย่าง + แก้ข้อมูลบริษัทผู้จ่าย + เพิ่ม/ลบผู้ใช้ + ยกเลิกเอกสาร |
| `user` | สร้าง/ค้นหา/พิมพ์เอกสาร, จัดการรายชื่อผู้ถูกหักภาษี |

---

## 1. ระบบฐานข้อมูลคลาวด์

- ใช้ Supabase (PostgreSQL) — ข้อมูลอยู่บนคลาวด์ สำรองอัตโนมัติโดยผู้ให้บริการ
- เข้าใช้ผ่านเบราว์เซอร์ (Chrome/Edge/Safari) รองรับทั้งคอมและมือถือ (มือถือเน้นดู/ค้นหา, การพิมพ์เน้นคอม)
- ล็อกอินด้วย **อีเมล + รหัสผ่าน** (Supabase Auth)
- ข้อมูลแยกตามองค์กร (`org_id`) — ป้องกันด้วย RLS ทุกตาราง
- มี Audit log บันทึกว่าใครสร้าง/แก้/ยกเลิกเอกสารเมื่อไหร่

### 1.1 โครงสร้างตาราง

#### `organizations` — ข้อมูลบริษัทผู้จ่ายเงิน (ผู้หักภาษี)
| คอลัมน์ | ชนิด | หมายเหตุ |
|---|---|---|
| id | uuid PK | |
| name | text | ชื่อผู้มีหน้าที่หักภาษี ณ ที่จ่าย |
| tax_id | char(13) | เลขประจำตัวผู้เสียภาษี |
| branch | text | สำนักงานใหญ่ / สาขาที่ (เก็บเป็นข้อความ เช่น "00000") |
| address | text | ที่อยู่เต็ม |
| logo_url | text | ไฟล์ใน Supabase Storage (ถ้ามี) |
| signature_url | text | รูปลายเซ็นผู้มีอำนาจ — **ยืนยันแล้วว่าต้องการ** อัปโหลดได้ในหน้าตั้งค่า ถ้าไม่อัปโหลดจะเว้นที่ว่างให้เซ็นสด |
| signer_name | text | ชื่อผู้ลงนาม |
| signer_position | text | ตำแหน่ง |
| created_at | timestamptz | |

#### `profiles` — ผู้ใช้ระบบ (ผูกกับ auth.users)
| คอลัมน์ | ชนิด | หมายเหตุ |
|---|---|---|
| id | uuid PK | = auth.users.id |
| org_id | uuid FK | |
| full_name | text | |
| role | text | `admin` / `user` |
| is_active | boolean | |
| created_at | timestamptz | |

#### `payees` — รายชื่อผู้ถูกหักภาษี ณ ที่จ่าย (ทะเบียนลูกค้า/ผู้รับเงิน)
| คอลัมน์ | ชนิด | หมายเหตุ |
|---|---|---|
| id | uuid PK | |
| org_id | uuid FK | |
| entity_type | text | `individual` (บุคคลธรรมดา) / `juristic` (นิติบุคคล) |
| tax_id | char(13) | บังคับ, ตรวจ 13 หลัก |
| title | text | คำนำหน้า (นาย/นาง/นางสาว/บริษัท/ห้างหุ้นส่วนจำกัด) |
| name | text | ชื่อ-นามสกุล หรือ ชื่อนิติบุคคล |
| branch | text | สำนักงานใหญ่ / สาขาที่ |
| address | text | ที่อยู่เต็ม (แสดงในเอกสาร) |
| phone, email | text | สำหรับติดต่อ (ไม่พิมพ์ในเอกสาร) |
| default_income_type | text | ประเภทเงินได้ที่ใช้บ่อยของรายนี้ (ช่วยกรอกเร็ว) |
| default_rate | numeric(5,2) | อัตรา % ที่ใช้บ่อย |
| note | text | |
| is_active | boolean | |
| created_at / updated_at / created_by | | |

**Index:** `(org_id, tax_id)` unique, และ index สำหรับค้นหาชื่อแบบ ILIKE

#### `income_types` — ตารางแม่ประเภทเงินได้ (master data, ตั้งค่าไว้ล่วงหน้า)
| คอลัมน์ | ชนิด | ตัวอย่าง |
|---|---|---|
| code | text PK | `40_1`, `40_2`, `40_3`, `40_4a`, `40_4b`, `tp4_rent`, `tp4_professional`, `tp4_contract`, `tp4_service`, `tp4_ads`, `tp4_transport`, `other` |
| section_no | int | หมายเลขข้อในแบบฟอร์ม (1-6) |
| label_th | text | ข้อความที่พิมพ์ลงเอกสาร |
| default_rate | numeric(5,2) | อัตราแนะนำ (ผู้ใช้แก้ได้เสมอ) |
| sort_order | int | |

> **สำคัญ:** `default_rate` เป็นเพียง *ค่าแนะนำเพื่อความสะดวก* ระบบต้องให้ผู้ใช้แก้ % ได้ทุกครั้ง และต้องมีข้อความกำกับในหน้าจอว่า "อัตราเป็นค่าตั้งต้น โปรดตรวจสอบกับกฎหมาย/ผู้ทำบัญชีก่อนออกเอกสาร"

#### `wht_certificates` — หัวเอกสาร 50 ทวิ
| คอลัมน์ | ชนิด | หมายเหตุ |
|---|---|---|
| id | uuid PK | |
| org_id | uuid FK | |
| doc_no | text | เลขที่เอกสาร (รูปแบบตามข้อ 6.2) — unique ต่อ org |
| book_no | text | เล่มที่ — **ยืนยันแล้ว** เป็นช่องกรอกอิสระ ผู้ใช้พิมพ์เองในฟอร์ม ระบบไม่ออกเลขให้ |
| form_type | text | `pnd1a`, `pnd1a_special`, `pnd2`, `pnd3`, `pnd2a`, `pnd3a`, `pnd53` |
| payee_id | uuid FK | อ้างอิงทะเบียน |
| payee_snapshot | jsonb | **สำเนาข้อมูลผู้ถูกหัก ณ วันที่ออก** (กันข้อมูลเปลี่ยนย้อนหลัง) |
| payer_snapshot | jsonb | สำเนาข้อมูลผู้จ่าย ณ วันที่ออก |
| issue_date | date | วันที่ออกหนังสือรับรอง |
| total_amount | numeric(15,2) | รวมเงินที่จ่าย |
| total_tax | numeric(15,2) | รวมภาษีที่หักและนำส่ง |
| total_tax_text | text | ตัวอักษรไทยของยอดภาษีรวม |
| pf_gpf_amount | numeric(15,2) | เงินที่จ่ายเข้า กบข./กสจ./กองทุนสงเคราะห์ครูฯ |
| sso_amount | numeric(15,2) | กองทุนประกันสังคม |
| provident_amount | numeric(15,2) | กองทุนสำรองเลี้ยงชีพ |
| payment_condition | text | `withheld` = หัก ณ ที่จ่าย, `paid_once` = ออกให้ครั้งเดียว, `paid_always` = ออกให้ตลอดไป, `other` = อื่น ๆ |
| payment_condition_other | text | |
| status | text | `draft` / `issued` / `void` |
| void_reason | text | |
| note | text | |
| created_by / created_at / updated_at | | |

#### `wht_certificate_items` — รายการเงินได้ในเอกสาร (1 เอกสารมีได้หลายบรรทัด)
| คอลัมน์ | ชนิด | หมายเหตุ |
|---|---|---|
| id | uuid PK | |
| certificate_id | uuid FK ON DELETE CASCADE | |
| income_type_code | text FK | |
| label_override | text | กรณีเลือก "อื่น ๆ" ให้พิมพ์เอง |
| paid_date | date | วัน เดือน ปี ที่จ่าย |
| amount | numeric(15,2) | จำนวนเงินที่จ่าย |
| rate | numeric(5,2) | % ที่หัก |
| tax_amount | numeric(15,2) | ภาษีที่หัก |
| sort_order | int | |

#### `doc_counters` — ตัวนับเลขที่เอกสาร
| org_id | period (text เช่น `2569`) | last_seq (int) | PK(org_id, period) |

#### `audit_logs`
| id | org_id | user_id | action (`create`/`update`/`void`/`print`) | entity | entity_id | payload jsonb | created_at |

### 1.2 กฎ RLS (ทุกตาราง)
```
เปิด RLS ทุกตาราง
policy: org_id = (select org_id from profiles where id = auth.uid())
เพิ่มเติม: ลบ (DELETE) wht_certificates อนุญาตเฉพาะ status='draft' และ role='admin'
```

---

## 2. ระบบจัดการรายชื่อผู้ถูกหักภาษี

**หน้าจอ: รายชื่อผู้ถูกหักภาษี**
- ตารางแสดง: ชื่อ / เลขผู้เสียภาษี / ประเภท / จำนวนเอกสารที่เคยออก / ใช้ล่าสุดเมื่อ
- ช่องค้นหาเดียว ค้นได้ทั้ง **ชื่อ** และ **เลขผู้เสียภาษี** (ค้นแบบพิมพ์ไปเจอไป, debounce 300ms)
- ปุ่ม: เพิ่มใหม่ / แก้ไข / ปิดใช้งาน (ไม่ลบจริง)
- นำเข้าจาก Excel — **ยืนยันแล้วว่าพักไว้ก่อน** ไม่อยู่ในขอบเขตเวอร์ชันแรก ถ้าลูกค้าต้องการภายหลังให้คิดเป็นงานเพิ่ม
- แบ่งหน้า 20 รายการ

**การดึงมาใช้ซ้ำ:** ในหน้าสร้างเอกสาร ช่อง "ผู้ถูกหักภาษี" เป็นแบบ autocomplete — พิมพ์ 2 ตัวอักษรขึ้นไปแล้วเลือก ระบบเติม ที่อยู่ / เลขภาษี / ประเภทเงินได้ที่ใช้บ่อย ให้อัตโนมัติ และมีปุ่ม "+ เพิ่มรายชื่อใหม่" ในหน้าเดียวกัน (modal) ไม่ต้องออกจากฟอร์ม

**การตรวจข้อมูล**
- เลขผู้เสียภาษีต้องเป็นตัวเลข 13 หลัก
- ถ้าเลขซ้ำกับรายชื่อเดิม ให้เตือน "มีรายชื่อนี้อยู่แล้ว: [ชื่อ]" พร้อมปุ่มไปเปิดรายการเดิม

---

## 3. ระบบสร้างเอกสาร 50 ทวิ

### 3.1 ฟอร์มกรอก (เรียงตามแบบฟอร์มจริง)
1. **ผู้มีหน้าที่หักภาษี** — ดึงจาก `organizations` อัตโนมัติ (แก้ไม่ได้ในฟอร์ม แก้ที่หน้าตั้งค่า)
2. **ผู้ถูกหักภาษี** — autocomplete ตามข้อ 2
3. **ลำดับที่ / เล่มที่ / เลขที่** — ระบบออกให้อัตโนมัติ แก้ทับได้
4. **แบบที่ยื่นรายการ** — เลือก 1 ใน: ภ.ง.ด.1ก / ภ.ง.ด.1ก พิเศษ / ภ.ง.ด.2 / ภ.ง.ด.3 / ภ.ง.ด.2ก / ภ.ง.ด.3ก / ภ.ง.ด.53
5. **รายการเงินได้ที่จ่าย** — ตารางเพิ่มได้หลายบรรทัด แต่ละบรรทัดมี:
   - ประเภทเงินได้ (dropdown จาก `income_types`)
   - วัน เดือน ปี ที่จ่าย
   - จำนวนเงินที่จ่าย
   - อัตรา % (ค่าตั้งต้นมาจากประเภทเงินได้ แก้ได้)
   - ภาษีที่หัก (คำนวณอัตโนมัติ, แก้ทับได้เพื่อกรณีปัดเศษพิเศษ)
   - ปุ่มลบบรรทัด
6. **รวมเงินที่จ่ายและภาษีที่หักนำส่ง** — คำนวณอัตโนมัติ
7. **รวมภาษีที่หักนำส่ง (ตัวอักษร)** — แปลงอัตโนมัติ อ่านอย่างเดียว
8. **เงินที่จ่ายเข้ากองทุน** — กบข./กสจ. , ประกันสังคม , กองทุนสำรองเลี้ยงชีพ
9. **ผู้จ่ายเงิน** — เลือก: หัก ณ ที่จ่าย / ออกให้ตลอดไป / ออกให้ครั้งเดียว / อื่น ๆ
10. **วันที่ออกหนังสือรับรอง** — ค่าตั้งต้น = วันนี้
11. ปุ่ม: **บันทึกร่าง** / **บันทึกและออกเอกสาร** / **บันทึกแล้วพิมพ์**

### 3.2 สูตรคำนวณ
```
tax_amount(บรรทัด) = ปัด( amount * rate / 100 , 2 ตำแหน่ง, แบบ half-up )
total_amount = ผลรวม amount ทุกบรรทัด
total_tax    = ผลรวม tax_amount ทุกบรรทัด
```
- คำนวณทันทีขณะพิมพ์ (on input)
- ถ้าผู้ใช้แก้ `tax_amount` เอง ให้ทำเครื่องหมายบรรทัดนั้นว่า "แก้ด้วยมือ" และหยุดคำนวณทับ จนกว่าจะกดปุ่ม "คำนวณใหม่"

### 3.3 แปลงตัวเลขเป็นตัวอักษรไทย (`src/lib/bahtText.js`)
กฎที่ต้องถูกต้อง:
| ตัวเลข | ผลลัพธ์ |
|---|---|
| 1000 | หนึ่งพันบาทถ้วน |
| 21 | ยี่สิบเอ็ดบาทถ้วน |
| 11 | สิบเอ็ดบาทถ้วน |
| 101 | หนึ่งร้อยเอ็ดบาทถ้วน |
| 1000000 | หนึ่งล้านบาทถ้วน |
| 1234567.89 | หนึ่งล้านสองแสนสามหมื่นสี่พันห้าร้อยหกสิบเจ็ดบาทแปดสิบเก้าสตางค์ |
| 0.5 | ห้าสิบสตางค์ |
| 0 | ศูนย์บาทถ้วน |
| 1000000000000 | (รองรับหลักล้านซ้อน) หนึ่งล้านล้านบาทถ้วน |

กฎย่อย (ยืนยันแล้ว 20 ส.ค. 2026): หลักสิบ "1" = "สิบ" (ไม่ใช่ "หนึ่งสิบ"), หลักสิบ "2" = "ยี่สิบ", หลักหน่วย "1" = "เอ็ด" ทุกครั้งที่จำนวนนั้นมีมากกว่า 1 หลัก ไม่ว่าหลักที่นำหน้าจะเป็นหลักสิบ ร้อย พัน หมื่น แสน หรือล้าน (เช่น 101 = หนึ่งร้อยเอ็ด, 1001 = หนึ่งพันเอ็ด, 100001 = หนึ่งแสนเอ็ด) ส่วนเลข 1 ที่อยู่โดด ๆ ยังอ่านว่า "หนึ่ง"
ต้องมีไฟล์ทดสอบ `bahtText.test.js` ครอบทุกเคสข้างบน

**ยืนยันแล้ว (21 ส.ค. 2026):** ตัวเลขในเอกสารใช้ **เลขอารบิก (123)** ทั้งหมด เจ้าของงานยืนยันว่าการใช้เลขไทยไม่ใช่ข้อบังคับตามกฎหมาย และเลือกเลขอารบิกเพราะตรงกับซอฟต์แวร์บัญชีทั่วไป ถือว่าคำถามข้อนี้ปิดแล้ว

---

## 4. ระบบประวัติเอกสาร (History)

**หน้าจอ: ประวัติเอกสาร**
- ตาราง: เลขที่ / วันที่ออก / ชื่อผู้ถูกหัก / แบบที่ยื่น / ยอดเงิน / ยอดภาษี / สถานะ
- ตัวกรอง: ช่วงวันที่, ผู้ถูกหักภาษี, แบบที่ยื่น (ภ.ง.ด.x), สถานะ
- ค้นหา: เลขที่เอกสาร / ชื่อ / เลขผู้เสียภาษี
- แถบสรุปด้านบน: จำนวนใบ, รวมยอดจ่าย, รวมยอดภาษี ตามตัวกรองปัจจุบัน
- ปุ่มต่อแถว: **ดู** / **พิมพ์ซ้ำ** / **คัดลอกเป็นใบใหม่** / **ยกเลิกเอกสาร (void)** / **แก้ไข** (เฉพาะสถานะ draft)
- ปุ่มรวม: **ส่งออก Excel/CSV** (สำหรับส่งผู้ทำบัญชียื่น ภ.ง.ด.3 / ภ.ง.ด.53)

**กฎสำคัญ:** เอกสารที่ `issued` แล้ว ห้ามแก้ตัวเลข — ถ้าผิดต้อง `void` แล้วออกใบใหม่ (ระบบต้องบันทึกเหตุผลการยกเลิก)

---

## 5. ระบบพิมพ์และ PDF

- ขนาดกระดาษ: **A4 แนวตั้ง** (`@page { size: A4 portrait; margin: 10mm; }`)
- ฟอนต์ Sarabun ทั้งหน้า
- พิมพ์ **1 ฉบับ = 1 หน้า** และมีตัวเลือกพิมพ์ต่อเนื่อง 2 หน้า คือ
  - ฉบับที่ 1 (สำหรับผู้ถูกหักภาษี ณ ที่จ่าย ใช้แนบพร้อมแบบแสดงรายการภาษี)
  - ฉบับที่ 2 (สำหรับผู้ถูกหักภาษี ณ ที่จ่าย เก็บไว้เป็นหลักฐาน)
- โหมดพิมพ์ 2 แบบ (สลับได้ในหน้าตั้งค่า):
  1. **พิมพ์ทั้งฟอร์ม** — ระบบวาดเส้นกรอบและข้อความหัวฟอร์มให้ครบ (ใช้กระดาษเปล่า)
  2. ~~**พิมพ์เฉพาะข้อมูล** — พิมพ์ทับแบบฟอร์มที่ซื้อมา~~ **ตัดออกแล้ว** ลูกค้ายืนยันว่าไม่มีแบบฟอร์มกระดาษเดิม จึงไม่ทำโหมดนี้และไม่เก็บค่าตำแหน่ง X/Y ในฐานข้อมูล
- ปุ่ม "บันทึก PDF" = เรียก `window.print()` แล้วแนะนำผู้ใช้เลือกปลายทางเป็น "Save as PDF"
- ห้ามให้ header/footer ของเบราว์เซอร์ทับเนื้อหา — ต้องมีคำแนะนำในหน้าจอว่าให้ปิด "Headers and footers" ในกล่องพิมพ์
- ทุกครั้งที่พิมพ์ บันทึก `audit_logs` action=`print`

---

## 6. กฎธุรกิจอื่น ๆ

### 6.1 สถานะเอกสาร
`draft` → `issued` → (`void`)
- `draft`: แก้ได้ ลบได้ **ยังไม่กินเลขที่เอกสารจริง** (ยืนยันแล้ว 20 ส.ค. 2026) เลขที่เอกสารจะถูกออกก็ต่อเมื่อกดปุ่ม "บันทึกและออกเอกสาร" เท่านั้น ใบร่างจึงไม่มี `doc_no` และการลบใบร่างทิ้งจะไม่ทำให้เลขที่ขาดช่วง
- `issued`: แก้ไม่ได้ พิมพ์ได้
- `void`: แสดงคำว่า "ยกเลิก" ทับบนเอกสารเวลาพิมพ์

### 6.2 รูปแบบเลขที่เอกสาร (ยืนยันแล้ว)
```
WHT-{ปี พ.ศ. 4 หลัก}-{ลำดับ 4 หลัก}    เช่น WHT-2569-0001
```
ตัวนับรีเซ็ตทุกปีภาษี ตั้งค่าเปลี่ยน prefix ได้ในหน้าตั้งค่า

### 6.3 อัตราภาษีตั้งต้นที่จะใส่ในตาราง `income_types`
> ใช้เป็นค่าแนะนำเท่านั้น ผู้ใช้แก้ได้ทุกครั้ง และต้องให้ลูกค้า/ผู้ทำบัญชีตรวจสอบยืนยันก่อนใช้งานจริง

| ประเภท | ข้อในฟอร์ม | อัตราแนะนำ |
|---|---|---|
| เงินเดือน ค่าจ้าง ม.40(1) | 1 | ตามอัตราก้าวหน้า (ให้กรอกเอง) |
| ค่าธรรมเนียม ค่านายหน้า ม.40(2) | 2 | กรอกเอง |
| ค่าแห่งลิขสิทธิ์ ม.40(3) | 3 | 3 |
| ดอกเบี้ย ม.40(4)(ก) | 4 ก | 1 |
| เงินปันผล ม.40(4)(ข) | 4 ข | 10 |
| ค่าเช่าทรัพย์สิน | 5 | 5 |
| ค่าวิชาชีพอิสระ | 5 | 3 |
| ค่ารับเหมา / ค่าจ้างทำของ | 5 | 3 |
| ค่าบริการ | 5 | 3 |
| ค่าโฆษณา | 5 | 2 |
| ค่าขนส่ง | 5 | 1 |
| อื่น ๆ (พิมพ์เอง) | 6 | กรอกเอง |

---

## 7. หน้าจอทั้งหมดในระบบ

| # | หน้า | path |
|---|---|---|
| 1 | เข้าสู่ระบบ | `/login` |
| 2 | หน้าหลัก (สรุปยอดเดือนนี้ + ปุ่มลัด) | `/` |
| 3 | สร้างเอกสาร 50 ทวิ | `/new` |
| 4 | ประวัติเอกสาร | `/history` |
| 5 | ดู/พิมพ์เอกสาร | `/doc/:id` |
| 6 | รายชื่อผู้ถูกหักภาษี | `/payees` |
| 7 | ตั้งค่าองค์กร + ผู้ใช้ + เลขที่เอกสาร | `/settings` |

---

## 8. แนวทาง UI (ให้เรียบ อ่านง่าย เหมาะงานเอกสารราชการ)

- โทน: พื้นขาว/เทาอ่อน + สีหลักน้ำเงินเข้ม (`#1B3A6B`) + สีเน้นเหลืองทอง (`#C9A227`) ใช้น้อย ๆ กับปุ่มหลัก
- ฟอนต์: Sarabun (400 / 600 / 700)
- ตัวอักษรขนาดใหญ่พอ (base 16px) เพราะผู้ใช้เป็นฝ่ายบัญชี/ธุรการ ใช้ทั้งวัน
- ทุกปุ่มสำคัญเขียนเป็นคำกริยาไทยชัดเจน: "บันทึกและออกเอกสาร", "พิมพ์เอกสาร", "เพิ่มรายชื่อ"
- ข้อความ error ต้องบอกว่าผิดตรงไหน + แก้อย่างไร ห้ามขึ้นแค่ "เกิดข้อผิดพลาด"
- หน้าจอว่าง (ยังไม่มีข้อมูล) ต้องมีปุ่มชวนเริ่มงาน
- รองรับคีย์บอร์ดล้วน (กด Tab ไล่ช่องได้ครบ, Enter = เพิ่มบรรทัดถัดไปในตารางรายการ)

---

## 9. สิ่งที่ยังไม่รวมในเวอร์ชันแรก (ตกลงกับลูกค้าให้ชัด)

- ไม่รวมการยื่นแบบ ภ.ง.ด. ออนไลน์ตรงกับกรมสรรพากร (ระบบทำได้แค่ export ไฟล์ให้)
- ไม่รวมการคำนวณภาษีเงินได้บุคคลธรรมดาแบบขั้นบันไดอัตโนมัติสำหรับเงินเดือน
- ไม่รวมระบบบัญชีเต็มรูปแบบ / ใบกำกับภาษี / ใบเสร็จ
- ไม่รวมแอปมือถือ (ใช้ผ่านเบราว์เซอร์บนมือถือได้ แต่ไม่ทำ native app)

---

## 10. คำถามที่ต้องถามลูกค้าก่อนเริ่มเขียนโค้ดจริง

ดูรายการเต็มใน `Project_Status.md` หัวข้อ "คำถามค้างคาที่ต้องถามลูกค้า"
