# DEPLOY_ENV.md — รายการค่าที่ต้องกรอกใน Netlify

> ไฟล์นี้เป็น **เอกสารอ้างอิงเท่านั้น ไม่มีค่าจริงอยู่ในไฟล์นี้**
> ค่าจริงอยู่ในไฟล์ `.env` บนเครื่องผู้พัฒนา ซึ่งถูกกันไม่ให้ขึ้น git ไว้แล้ว
> **ห้ามเติมค่าจริงลงในไฟล์นี้เด็ดขาด** เพราะไฟล์นี้จะถูกอัปขึ้น git ไปด้วย

---

## ค่าที่ต้องกรอกใน Netlify

เว็บนี้ต้องการค่าเพียง **2 ตัว** เท่านั้น ทั้งคู่หาได้จากหน้าเว็บ Supabase

| ชื่อตัวแปร | คืออะไร | หาได้จากไหน |
|---|---|---|
| `VITE_SUPABASE_URL` | ที่อยู่ของฐานข้อมูลบนคลาวด์ | Supabase → **Project Settings** → **API** → หัวข้อ **Project URL** |
| `VITE_SUPABASE_ANON_KEY` | กุญแจสาธารณะสำหรับให้เว็บติดต่อฐานข้อมูล | Supabase → **Project Settings** → **API** → หัวข้อ **Project API keys** → บรรทัด **anon / public** |

---

## วิธีกรอกใน Netlify (ทำหลังสมัครบัญชีและเชื่อมโปรเจกต์แล้ว)

1. เข้าหน้าโปรเจกต์ใน Netlify แล้วกดเมนู **Site configuration**
2. เลือกหัวข้อ **Environment variables** ทางด้านซ้าย
3. กดปุ่ม **Add a variable** แล้วเลือก **Add a single variable**
4. ช่อง **Key** ใส่ `VITE_SUPABASE_URL` ช่อง **Value** วางค่าที่คัดลอกมาจาก Supabase
5. ทำซ้ำข้อ 3-4 อีกครั้งสำหรับ `VITE_SUPABASE_ANON_KEY`
6. กด **Save** แล้วสั่ง deploy ใหม่อีกครั้ง (**Deploys** → **Trigger deploy** → **Clear cache and deploy site**)

> **สำคัญ:** ต้อง deploy ใหม่หลังเพิ่มค่า เพราะค่าเหล่านี้ถูกฝังเข้าไปในไฟล์เว็บตอน build
> ถ้าเพิ่มค่าแล้วไม่ deploy ใหม่ เว็บจะยังขึ้นข้อความว่า "ยังเชื่อมต่อฐานข้อมูลไม่ได้" เหมือนเดิม

---

## เรื่องความปลอดภัยที่ต้องรู้

**กุญแจ `anon key` เปิดเผยได้ ไม่ใช่ความลับ** ใครเปิดดูโค้ดเว็บก็เห็นได้ และเป็นเรื่องปกติ
สิ่งที่ป้องกันข้อมูลจริงคือกฎ **RLS** ในฐานข้อมูล ซึ่งบังคับว่าผู้ใช้ต้องล็อกอินก่อน
และเห็นได้เฉพาะข้อมูลขององค์กรตัวเองเท่านั้น

**ห้ามนำ `service_role key` มาใส่ที่นี่หรือที่ใดก็ตามในเว็บเด็ดขาด**
กุญแจตัวนั้นข้ามกฎความปลอดภัยทั้งหมดได้ ถ้าหลุดออกไปคือใครก็อ่านและลบข้อมูลได้ทั้งฐาน
ระบบนี้ไม่มีส่วนไหนต้องใช้กุญแจตัวนั้นเลย

---

## ตรวจสอบว่ากรอกถูกแล้ว

หลัง deploy เสร็จ เปิดเว็บที่อยู่จริงที่ Netlify ให้มา แล้วดูว่า

- ถ้าเห็น **หน้าเข้าสู่ระบบ** = กรอกค่าถูกต้องแล้ว
- ถ้าเห็นข้อความ **"ยังเชื่อมต่อฐานข้อมูลไม่ได้"** = ค่ายังไม่ครบ หรือยังไม่ได้ deploy ใหม่หลังเพิ่มค่า
