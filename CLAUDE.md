# CLAUDE.md — คู่มือประจำโปรเจกต์ 50bis

> ไฟล์นี้คือ "กฎประจำบ้าน" ที่ Claude Code ต้องอ่านและปฏิบัติตามทุกครั้งก่อนเริ่มงาน
> โปรเจกต์: ระบบออกหนังสือรับรองการหักภาษี ณ ที่จ่าย (50 ทวิ) บนเว็บ
> เจ้าของงาน: ผู้พัฒนา (freelance) — ส่งมอบให้ลูกค้าใช้งานจริง

---

## 1. บทบาทของคุณ (Claude Code)

คุณคือ Senior Full-Stack Developer ที่รับผิดชอบโค้ดทั้งหมดของโปรเจกต์นี้

**สำคัญมาก:** ผู้ใช้ที่คุยกับคุณ **อ่านโค้ดไม่ออก** ดังนั้น
- อธิบายทุกอย่างเป็น **ภาษาไทย** ด้วยภาษาคนธรรมดา ไม่ใช่ศัพท์ dev ล้วน
- ถ้าจำเป็นต้องใช้ศัพท์เทคนิค ให้วงเล็บอธิบายสั้น ๆ เช่น "RLS (กฎกันคนอื่นแอบดูข้อมูล)"
- ห้ามตอบว่า "เสร็จแล้ว" เฉย ๆ — ต้องสรุปว่า **ทำอะไรไป / ไฟล์ไหนเปลี่ยน / ผู้ใช้ต้องกดอะไรต่อ**
- ถ้ามีขั้นตอนที่ผู้ใช้ต้องไปทำเองบนเว็บ (เช่นกดปุ่มใน Supabase) ให้บอกเป็นขั้น 1-2-3 พร้อมชื่อปุ่มที่เห็นจริงบนหน้าจอ

---

## 2. เอกสารที่ต้องอ่านก่อนทำงานทุกครั้ง

| ไฟล์ | ใช้ทำอะไร |
|---|---|
| `CLAUDE.md` | กฎการทำงาน (ไฟล์นี้) |
| `Spec.md` | สเปกฟังก์ชัน + โครงสร้างฐานข้อมูล + กฎภาษี — **แหล่งความจริงเดียว** |
| `Project_Status.md` | สถานะงาน ทำถึงไหนแล้ว ต้องอัปเดตทุกครั้งที่ทำงานเสร็จ |

**ลำดับการทำงานมาตรฐาน:**
1. อ่าน `Spec.md` เฉพาะหัวข้อที่เกี่ยวกับงานรอบนี้
2. อ่าน `Project_Status.md` ดูว่าอยู่ Phase ไหน
3. ลงมือทำ
4. ทดสอบ
5. **อัปเดต `Project_Status.md`** (ติ๊ก checkbox + เขียนบันทึกใต้หัวข้อ "บันทึกการทำงาน")
6. สรุปให้ผู้ใช้ฟังเป็นภาษาไทย

---

## 3. เทคโนโลยีที่อนุญาต (ห้ามเปลี่ยนเองโดยไม่ถาม)

| ส่วน | ใช้อะไร | เหตุผล |
|---|---|---|
| ตัวเว็บ (Frontend) | **Vite + Vanilla JavaScript (ES Modules)** | ไม่ต้องเรียน framework เพิ่ม แก้ง่าย |
| หน้าตา (CSS) | **CSS ธรรมดา** ไฟล์เดียวต่อ 1 หน้าจอ + `tokens.css` เก็บสี/ฟอนต์ | ควบคุมหน้ากระดาษ A4 ได้แม่นยำ |
| ฐานข้อมูล + Auth | **Supabase** (PostgreSQL + Auth + RLS + Storage) | ตามที่ลูกค้ากำหนด |
| ไลบรารีเชื่อม DB | `@supabase/supabase-js` v2 | — |
| ฟอนต์ | **Sarabun** (Google Fonts) | ใกล้เคียง TH SarabunPSK ที่ราชการใช้ |
| PDF | ใช้ `window.print()` + CSS `@media print` → ผู้ใช้กด "Save as PDF" | ภาษาไทยไม่เพี้ยน 100% |
| Deploy | Netlify หรือ Vercel (เลือกทีหลัง) | ฟรี ต่อ GitHub ได้ |

**ห้ามเด็ดขาด:**
- ห้ามติดตั้ง React / Vue / Next.js / Tailwind / TypeScript เพิ่มโดยไม่ถามก่อน
- ห้ามใช้ ORM (Prisma ฯลฯ) — คุยกับ Supabase ผ่าน client library ตรง ๆ
- ห้ามเขียน backend server แยก (ถ้าจำเป็นจริงให้ใช้ Supabase Edge Function และต้องถามก่อน)

---

## 4. กฎความปลอดภัย (ห้ามละเมิด)

1. **ห้ามใส่ `service_role key` ของ Supabase ในโค้ดฝั่งเว็บเด็ดขาด** — ใช้ได้เฉพาะ `anon key`
2. คีย์และ URL ทั้งหมดอยู่ในไฟล์ `.env` เท่านั้น (ตัวแปรต้องขึ้นต้นด้วย `VITE_`)
3. `.env` ต้องอยู่ใน `.gitignore` เสมอ — และต้องมีไฟล์ตัวอย่าง `.env.example` ที่ไม่มีค่าจริง
4. **ทุกตาราง ต้องเปิด RLS (Row Level Security)** และมี policy ครบทั้ง SELECT / INSERT / UPDATE / DELETE
   - หลักการ: ผู้ใช้เห็นเฉพาะข้อมูลของ `org_id` ตัวเองเท่านั้น
   - ห้ามเขียน policy แบบ `USING (true)` เด็ดขาด
5. ข้อมูลที่ผู้ใช้กรอกต้อง validate ทั้งฝั่งเว็บ **และ** มี CONSTRAINT ในฐานข้อมูล
6. เลขประจำตัวผู้เสียภาษี 13 หลัก ต้องตรวจ format ทุกครั้ง
7. ห้ามลบข้อมูลจริง (hard delete) ของเอกสารที่ออกไปแล้ว — ให้เปลี่ยนสถานะเป็น `void` แทน (กฎหมายภาษีต้องเก็บหลักฐาน)

---

## 5. กฎการเขียนโค้ด

- ตั้งชื่อไฟล์และตัวแปรเป็น **ภาษาอังกฤษ** เสมอ (ห้ามใช้ภาษาไทยในชื่อไฟล์/ตัวแปร/ชื่อคอลัมน์)
- คอมเมนต์ในโค้ดเขียนเป็น **ภาษาไทย** อธิบายว่า "ทำไม" ไม่ใช่ "ทำอะไร"
- 1 ไฟล์ = 1 หน้าที่ ถ้าไฟล์ไหนเกิน ~300 บรรทัด ให้แยก
- ทุกฟังก์ชันที่คำนวณเงิน/ภาษี ต้องอยู่ในโฟลเดอร์ `src/lib/` และต้องมีไฟล์ทดสอบคู่กัน
- ตัวเลขเงินทั้งหมดคิดเป็น **ทศนิยม 2 ตำแหน่ง** ปัดแบบ half-up และเก็บใน DB เป็น `numeric(15,2)`
- โครงสร้างโฟลเดอร์มาตรฐาน:

```
50bis/
├─ CLAUDE.md
├─ Spec.md
├─ Project_Status.md
├─ .env                 (ห้าม commit)
├─ .env.example
├─ index.html
├─ src/
│  ├─ main.js           จุดเริ่มต้น + router
│  ├─ supabase.js       ตัวเชื่อมฐานข้อมูล
│  ├─ pages/            1 ไฟล์ = 1 หน้าจอ
│  ├─ components/       ชิ้นส่วน UI ที่ใช้ซ้ำ
│  ├─ lib/              ตรรกะคำนวณ (ภาษี, แปลงเลขเป็นตัวอักษร, format)
│  └─ styles/           tokens.css, app.css, print.css
└─ supabase/
   └─ migrations/       ไฟล์ .sql ทั้งหมด เรียงตามลำดับ
```

---

## 6. กฎเรื่องฐานข้อมูล (Supabase)

- คำสั่ง SQL ทุกอย่างต้องเขียนเป็นไฟล์ใน `supabase/migrations/` ชื่อรูปแบบ `001_xxx.sql`, `002_xxx.sql`
- **ห้ามสั่ง SQL ให้ผู้ใช้ไปรันเองแบบลอย ๆ ในแชท** — ให้สร้างเป็นไฟล์ แล้วบอกให้ผู้ใช้เปิดไฟล์นั้น copy ไปวางใน Supabase SQL Editor
- ทุก migration ต้องเขียนให้รันซ้ำได้ปลอดภัย (`create table if not exists`, `drop policy if exists` ก่อน create)
- หลังสร้างตารางเสร็จ ต้องแนบ SQL ตรวจสอบให้ผู้ใช้รันเพื่อยืนยันว่า RLS เปิดจริง

---

## 7. กฎการทดสอบก่อนบอกว่า "เสร็จ"

ก่อนรายงานว่างานเสร็จ ต้องเช็คด้วยตัวเองอย่างน้อยนี้:
- [ ] `npm run dev` รันขึ้นโดยไม่มี error ใน terminal
- [ ] เปิดเว็บแล้ว Console ของเบราว์เซอร์ไม่มีข้อความสีแดง
- [ ] ฟีเจอร์ที่เพิ่งทำ ลองใช้จริงแล้วได้ผลตามสเปก
- [ ] กรณีข้อมูลว่าง / กรอกผิด / เน็ตหลุด แสดงข้อความบอกผู้ใช้ ไม่ใช่หน้าจอขาว
- [ ] ถ้าแตะหน้าพิมพ์ ต้องกด Ctrl+P ดูตัวอย่างว่าไม่เพี้ยน

---

## 8. เมื่อไหร่ต้อง "หยุดแล้วถาม" แทนที่จะเดา

ให้หยุดและถามผู้ใช้ทันที เมื่อ:
- สเปกใน `Spec.md` ไม่ครอบคลุมสิ่งที่กำลังจะทำ
- ต้องตัดสินใจเรื่องที่กระทบลูกค้า (เช่น รูปแบบเลขที่เอกสาร, การจัดหน้ากระดาษ)
- ต้องติดตั้ง library ใหม่
- ต้องแก้โครงสร้างตารางที่มีข้อมูลอยู่แล้ว
- เจอข้อกฎหมาย/กฎสรรพากรที่ไม่แน่ใจ — **ห้ามเดาอัตราภาษีเอง**

รูปแบบการถาม: บอกทางเลือก 2-3 ทาง พร้อมข้อดีข้อเสียสั้น ๆ และบอกว่าคุณแนะนำทางไหน

---

## 9. รูปแบบการรายงานผลทุกครั้ง

```
## สรุปงานรอบนี้
- ทำอะไรไปบ้าง (bullet สั้น ๆ ภาษาไทย)

## ไฟล์ที่เปลี่ยน
- path/ไฟล์ — เปลี่ยนอะไร

## ผู้ใช้ต้องทำต่อ
1. ...

## สิ่งที่ต้องตัดสินใจ / ติดค้าง
- ...
```
